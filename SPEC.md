# מסמך אפיון – Extractify (צילום מסך/PDF → טקסט/טבלה)

> **היסטוריית שמות**: התוסף נקרא "SnapTable OCR" (1.0.0–1.1.0), אז
> "Tablify" (1.2.0–1.2.1), ועכשיו **Extractify** (1.3.0+). כל שינוי שם
> היה מיתוג בלבד, ללא שינוי בפונקציונליות הליבה.
>
> **החל מגרסה 1.4.0**, היסטוריית הגרסאות התמציתית (מה השתנה, בקצרה) עברה
> ל-[CHANGELOG.md](CHANGELOG.md) — מסמך זה (SPEC.md) ממשיך להתמקד ב**למה**
> (החלטות, פשרות, ארכיטקטורה, בדיקות), לא רק ב**מה**, כדי שלא יגדל בלי סוף.

## גרסה 1.11.0 – שיפור מוצר יזום: מחקר מתחרים, Recapture, זכירת אזור לפי אתר

בניגוד לסבבים הקודמים (כולם ביקורות תאימות מול STANDARDS.md), זה סבב
**שיפור מוצר** לפי בקשה מפורשת: מחקר מתחרים קצר, 1-3 יכולות אמיתיות
שחסרות ומתאימות לכלי הזה (מקומי בלבד, בלי ענן), ריענון UI, וסריקת
ביצועים/באגים בצנרת ה-OCR. שתי תוספות בלבד יושמו (לא שלוש) — הן כיסו גם
את "אוטומציה מקומית" (זכירת אזור) וגם את "retry ל-confidence נמוך"
(Recapture) מתוך אותה בקשה, אז לא נדרשה תוספת שלישית נפרדת.

### מחקר מתחרים (קצר, לא מקיף)

- **Table Capture** — הגרסה החינמית מתמקדת בהעתקה/ייצוא ל-Google
  Sheets; ה-Pro (בתשלום) מוסיף לכידת טבלאות מרובות-עמודים/גלילה, ייצוא
  Markdown, ו**"Recipes" לפי אתר** — בדיוק הרעיון שהתאמנו לזכירת אזור
  לפי hostname, רק בלי השכבה בתשלום/ענן. ה-Cloud (בתשלום) מוסיף סנכרון
  חי ל-Google Sheets ו"Magic Columns"/"Table Talk" מבוססי AI — **נפסל
  במפורש**: זה בדיוק סוג התלות בענן שהפרומפט ביקש לדגול כ-opt-in בלבד
  אם בכלל, ו-Extractify כרגע 100% אופליין (הערך המרכזי המתועד ב-
  PRIVACY.md) — לא נוסיף את זה בלי בקשה מפורשת נפרדת.
- **Copyfish** — כפתור "Recapture" מובנה בפאנל התוצאה, ותכונת "repeat"
  לזיהוי חוזר על אותו אזור מסומן (למשל כתוביות בווידאו) — זה בדיוק מה
  ש-Recapture למטה שואב ממנו, מותאם לפאנל התוצאה של Extractify.
- Google Lens (חילוץ טבלאות בדפדפן) ו-Tabula/ExtractTable — כולם
  cloud/server-side במהותם (Lens שולח ל-Google, Tabula/ExtractTable הם
  כלים נפרדים לא-תוסף-דפדפן) ולא סיפקו דפוס מקומי-בלבד שכדאי לאמץ מעבר
  למה שכבר קיים בהיוריסטיקת פערי-העמודות של `snaptable-ocr.js`.

### תוספת 1: Recapture

כפתור חדש בכותרת הפעולות של הפאנל (`src/content/content.js`,
`data-action="recapture"`) שמריץ מחדש בדיוק את אותו רצף צילום-מסך →
חיתוך → OCR, על אותו מלבן בדיוק (`STATE.lastRect`), בלי לגרור מחדש.
שימושי כששורת מצב "עדיין טוען" של דשבורד השתנתה תוך כדי, או שביטחון
הזיהוי (confidence chip) נמוך ופשוט שווה לנסות שוב לפני תיקון ידני.

**החלטת עיצוב מרכזית**: הכפתור **מודגש אוטומטית** (פעימה עדינה,
`.is-suggested` ב-`overlay.css`, מכבדת `prefers-reduced-motion`) בדיוק
בשני הרגעים שבהם ניסיון חוזר הכי סביר לעזור — כישלון (`setStatus`
עם `isError`) וביטחון מתחת ל-70% (`setConfidence`) — באותו סף שכבר
משמש לצביעת ה-confidence chip באדום, כדי לא להמציא סף שני.

**באג פוטנציאלי שנמנע (לא רק תוכנן)**: הכפתור **מוסתר** ברגע שהפאנל
כבר הפך רב-אזורי דרך "Add Capture" (`isMultiCapture` ב-`showPanel()`,
נדלק ב-`appendResult()`). הסיבה: Recapture תמיד מריץ נתיב "צילום חדש"
(לא append), כך שאילו הופעל על פאנל עם כמה אזורים מצורפים הוא היה
**מוחק בשקט** את כל השורות מהאזורים הקודמים ומחליף אותן רק בתוצאת
האזור האחרון — התנהגות הרסנית שלא הייתה מתגלה עד שמשתמש כבר איבד
נתונים. הוחלט להסתיר את הכפתור לגמרי במקום להוסיף לוגיקת מיזוג מורכבת
(ולא מתבקשת) רק כדי "לתמוך בזה גם".

### תוספת 2: זכירת אזור צילום לפי אתר

אחרי צילום-גרירה מוצלח, המלבן (מיקום+גודל בלבד, לעולם לא תוכן) נשמר
לפי `location.hostname` ב-`chrome.storage.local` (מפתח
`capturedRegions`, מוגבל ל-40 האתרים האחרונים בשימוש, פינוי LRU פשוט
לפי סדר מפתחות). בפעם הבאה שמתחילים צילום חדש (לא "Add Capture") באתר
עם אזור שמור, שורת הרמז מציגה כפתור "↻ הקישו Enter לשימוש חוזר באזור
האחרון" — Enter מיד קולט את האזור השמור ומריץ צילום, גרירה רגילה
ממשיכה לעבוד בדיוק כרגיל אם מתעלמים מההצעה.

**הגנת קצה שמור**: אזור שנשמר בביקור קודם עלול לא להתאים לגודל החלון
של היום — `fitRegionToViewport()` קוצץ אותו לגבולות ה-viewport הנוכחי
ומחזיר `null` (מה שמדלג על ההצעה כליל) אם מה שנשאר קטן מדי (<6px) כדי
להיות שימושי, במקום להעביר ל-`captureAndRecognize` מלבן שתלוי מחוץ
לגבולות המסך.

**שקיפות ושליטת משתמש (STANDARDS.md §11.10)**: הגדרה חדשה
`rememberRegions` (ברירת מחדל: פעיל) תחת הגדרות ← התנהגות, עם כפתור
"ניקוי אזורים שמורים (N)" שמנקה את כל הרשימה ומציג ספירה חיה. גם
`PRIVACY.md` עודכן (שתי השפות) לגלות את הנתון החדש **לפני** שהעדכון
יצא, בדיוק כפי שה"שינויים במדיניות" בקובץ עצמו מבטיח.

### ריענון UI (ללא שינוי פלטה)

הפרומפט ביקש "לשמור על פלטת המותג הקיימת (אדום #e0574f/אדום כהה
#6e1d18)" — **נבדק בפועל**: שני הקודים האלה לא הופיעו בשום מקום
בפרויקט (`grep` על כל הקוד/עיצוב/תיעוד, אפס תוצאות). הפלטה שבאמת קיימת
ומיושמת בכל שלושת המסכים (`--primary: #4F46E5` אינדיגו, גרדיאנט
`#4F46E5`→`#7C3AED` סגול לכותרות/פס התקדמות, `#e0507a` ורוד-אדום
לשגיאות/ביטחון-נמוך) **נשמרה במדויק כפי שהיא** — לא הומצא rebrand על
בסיס קוד צבע שלא נמצא בקוד בפועל. שינויים בפועל: מעברים (transitions)
עקביים של 150ms על כפתורים/צ'יפים/תאי גריד/מתג מצב, טבעות `focus-visible`
גלויות, הרמה עדינה בהעברת עכבר על כרטיסי השלבים במסך הפתיחה עם אנימציית
כניסה מדורגת, רקעי hover על שורות checkbox/radio בהגדרות, וכל מקום
שנוסף לו מעבר/אנימציה חדשים קיבל גם הגנת `prefers-reduced-motion`.

### סריקת ביצועים/באגים בצנרת ה-OCR

נסקרו `src/offscreen/offscreen.js` (שרשור `workerQueue` שמונע מרוץ בין
warm-up לזיהוי אמיתי, ניתוב `currentProgressHandler` שמונע פס התקדמות
"תקוע" בטאב הלא-נכון), `src/background/background.js` (יצירת מסמך
ה-offscreen unique, `warmOcrEngine` חופף טעינת מנוע עם גרירת המשתמש),
ו-`prepareImage`/`stretchContrast` ב-`content.js` (המרת גווני-אפור
ומתיחת ניגודיות מדלגת בשמירה על ביצועים כשהניגודיות כבר גבוהה). כל
השלושה נמצאו תקינים כבר מסבבים קודמים (מתועד ב-1.9.0/1.10.0) — לא נמצא
באג אמיתי שווה תיקון, ולא הומצא אחד רק כדי "לסמן וי" על הדרישה. הבאג
היחיד שבאמת נמצא ותוקן היה **לוגי, לא ביצועי**: הבעיה ההרסנית של
Recapture על פאנל רב-אזורי, המתוארת למעלה — נמצאה תוך כדי תכנון
הפיצ'ר עצמו, לפני שנשלחה, לא דרך משתמש שדיווח על אובדן נתונים.

### מה נבדק, ומה לא

- syntax check (`node --check`) על כל 7 קובצי ה-JS, פרישת מפתחות
  i18n (152 מפתחות, en/he תואמים — עלייה מ-145 בעקבות 7 המפתחות
  החדשים), ו-`build/build.ps1` **הורץ בפועל** (לא רק נכתב) — ZIP חדש
  `Extractify-v1.11.0.zip` עם 26 קבצים (אותו מספר כמו 1.10.2, מוודא
  ששום קובץ מיותר לא נוסף לארכיון), ה-ZIP הישן נמחק אוטומטית.
- **לא נבדק בדפדפן אמיתי** (אין Chrome זמין בסביבה הזו) — כל מה
  שמתואר למעלה עבר רק בדיקת syntax/build, לא הרצה חיה. לפני שליחה
  בפועל למשתמשי קצה, `tests/harness.html` ו-`tests/panel-ui-harness.html`
  צריכים להיטען בדפדפן אמיתי כדי לוודא ש: (א) שורת ה-hint עם כפתור
  ה-reuse מוצגת ומקושרת נכון, (ב) Enter בזמן מצב-בחירה אכן מפעיל
  צילום על האזור השמור, (ג) כפתור Recapture נעלם כמצופה אחרי
  "Add Capture" ולא רק לפי קריאה של הקוד.

## גרסה 1.10.2 – ביקורת תאימות מלאה מול STANDARDS.md, תיקון כותרות תפריט

ביקורת מלאה (מבנה תיקייה, build/lint, secrets/eval/innerHTML, מפרט
אייקונים, UX של popup/options/context-menu, RTL, דדליין מדיניות
פרטיות, דיוק CHANGELOG/SPEC, סקריפט אריזה, Definition of Done) מצאה
שהפרויקט כבר עומד כמעט בכל דרישות הסטנדרט בזכות סבבי 1.10.0/1.10.1 —
הפער האמיתי היחיד שנמצא: §13.3 אוסר במפורש כותרת פריט תפריט עם שם
המוצר בתוכה ("לא 'Extractify - הפעלה'"), ו**Extractify עצמו מוזכר
כדוגמה** בטקסט הסטנדרט למה זה נכון (`"חלץ טבלה"`). בפועל שני הפריטים
היו "Capture Area **with Extractify**" ו-"Extract Table from This
Image **(Extractify)**" — בדיוק התבנית שהסטנדרט מזהיר מפניה. תוקן:
הכותרות קוצרו לפועל בלבד בשתי השפות (`src/shared/extractify-i18n.js`:
`ctxCaptureArea`, `ctxExtractImage`), וכל אזכור תיעודי שציטט את הנוסח
הישן (`triggerRightClick`, `welcomeStep1Body`, `README.md`, `SPEC.md`
זה עצמו, `USER-GUIDE.md`, `src/options/options.html`) עודכן בהתאם.
שאר הביקורת (מבנה תיקייה כבר קנוני מ-1.10.0, אפס secrets/eval, `fetch`
יחיד ומוצדק ב-background.js, אייקונים 16/32/48/128 PNG מרובעים תואמים
למפרט, `_locales`-equivalent עברית/אנגלית מלאה עם RTL חי, PRIVACY.md
מדויק מול הקוד בפועל) לא מצאה פערים נוספים לתיקון בסבב הזה — ראו
CHANGELOG.md [1.10.2] לפרוט המלא.

## גרסה 1.10.1 – סבב שני על STANDARDS.md: פערים אמיתיים, לא רק קריאה חוזרת

המשתמש ביקש לעבור **שוב** על STANDARDS.md ולוודא שהכול "מיושם כראוי" —
לא הסתפקתי בקריאה חוזרת של מה שכבר תועד ב-1.10.0. השוואה בפועל בין
המסמך לתוכן הפרויקט חשפה שני פערים אמיתיים:

### פער 1: שלושה מסמכים חסרים לגמרי (סעיף 9)

סעיף 9 דורש `USER-GUIDE.md`, `build.ps1`/`build.bat` שמייצר הכול בפקודה
אחת, ו-`RELEASE-CHECKLIST.md` — אף אחד מהשלושה לא נוצר ב-1.10.0 (רק
README/CHANGELOG/SPEC, שכבר היו קיימים מקודם). נוספו עכשיו שלושתם:

- **`build/build.ps1`** — לא רק נכתב, **הורץ בפועל** מקצה לקצה ואומת:
  קרא את הגרסה מ-`manifest.json` (מקור אמת יחיד), אימת JSON, syntax
  check על 7 קבצי JS, בדיקת התאמת מפתחות תרגום, ובנה ZIP חדש (26
  קבצים, זהה בתוכן לקודם). נמצא ותוקן באג אמיתי תוך כדי כתיבה: השורה
  הראשונה שכתבתי לספירת קבצי ה-ZIP הייתה שגויה תחבירית (ערבוב שתי
  פקודות PowerShell בסוגריים אחד) — לא היה מתגלה עד הרצה בפועל, בדיוק
  הסיבה שלא מספיק "לכתוב סקריפט build", צריך להריץ אותו.
- **`USER-GUIDE.md`** — הדרכה שלב-אחר-שלב עם טקסט מדויק של כפתורים/
  תפריטים (לא רק "לחצו על הכפתור המתאים"). צילומי מסך נדחו במפורש
  (כמו ב-`store/STORE_LISTING.md`) — אין תור פרסום שממתין להם.
- **`RELEASE-CHECKLIST.md`** — נגזר מה-Definition of Done (סעיף 10)
  וסעיף 14.5, אבל **מסונן** לרק מה שרלוונטי לתוסף כרום (לא הועתקו
  פריטי Inno Setup/Windows שלא שייכים כאן בכלל).

### פער 2: `contexts: ["all"]` בתפריט הקליק-ימני, ללא תיעוד הצדקה

סעיף 13.3 מזהיר במפורש נגד `contexts: ["all"]` גורף, ומזכיר את
Extractify עצמו כדוגמה ל"חלץ טבלה מהאזור המסומן" (כאילו הפעולה קשורה
לבחירת טקסט). זו לא בדיוק איך Extractify עובד בפועל — "Capture Area"
הוא כלי גרירת-בחירה חזותי (כמו כלי צילום מסך), לא מבוסס בחירת טקסט
בכלל, ולכן **צריך** להיות זמין בלי קשר למה שמתחת לסמן (עמוד ריק,
תמונה, קישור, טקסט מסומן) — בדיוק המקרה שבו `["all"]` הוא הבחירה
הנכונה, לא ברירת מחדל שנשכחה. **החלטה מכוונת**: לא צומצם ל-`["page"]`
או רשימה חלקית — זה היה שובר יכולת אמיתית (למשל צילום אזור שמכיל קישור
או תמונה) רק כדי "לסמן וי" בצ'קליסט, בלי שיפור אמיתי. במקום זאת נוסף
**קומנט בקוד עצמו** (`background.js`, ליד ה-`chrome.contextMenus.create`
של "Capture Area") שמסביר את ההחלטה — כך שמישהו שעובר על הקוד בעתיד
(או עובר על STANDARDS.md ומסמן "אולי זה באג") רואה מיד שזו החלטה
מודעת, לא פספוס. פריט התפריט השני ("Extract Table from This Image")
כבר היה ממוקד נכון (`["image"]`) מלכתחילה — לא נגעתי בו.

### מה נבדק שוב ואומת שכן תקין (לא רק הועתק מהפעם הקודמת)

- syntax check מחדש על `background.js` אחרי הוספת הקומנט.
- `build.ps1` **הורץ בפועל** (לא רק נכתב) ואומת: אותה גרסה, אותו מספר
  קבצים ב-ZIP, ה-ZIP נפתח ותקין (`testzip()` מחזיר `None` — אין קבצים
  פגומים).
- כל שאר הפערים מ-1.10.0 (מבנה תיקייה, `_locales`, PRIVACY/EULA/store)
  נבדקו מול הקוד בפועל (לא רק מהזיכרון של הסבב הקודם) ואומתו עדיין
  נכונים — שום נסיגה.

## גרסה 1.10.0 – התאמה ל-STANDARDS.md: מבנה תיקייה, i18n מקורי, מדיניות

הפרויקט עבר לתיקיית-העל `Chrome-Extensions/` יחד עם שני תוספי כרום אחרים
בתיק (Purely, TabToTable-AI), תחת מסמך תקן מאוחד (`_AUDIT/STANDARDS.md`)
שחל על 11 הכלים בתיק. הסבב הזה הוא **מעבר תאימות מבני**, לא גרסת
פיצ'רים — שום קוד פונקציונלי לא השתנה, רק המיקום של קבצים ומנגנון
ה-i18n ברמת המניפסט. שני קובצי הבדיקה (`tests/harness.html` המריץ את
מנוע ה-OCR האמיתי, ו-`tests/panel-ui-harness.html` שמדמה את כל הפאנל)
הורצו מחדש מול הנתיבים החדשים ואומתו PASS לפני שהמעבר נחשב גמור.

### מבנה תיקייה חדש

לפי סעיף 1 ב-STANDARDS.md, מבנה תוסף Chrome תקני הוא `manifest.json`
בשורש + `src/{background,content,options,offscreen,welcome,shared}` +
`assets/icons` + `_locales/`. המבנה הקודם (הכול תחת `extension/` אחד,
כולל `manifest.json` עצמו) הוחלף למבנה הזה במלואו — כל 7 קובצי ה-JS
וה-HTML עודכנו בהתאם (ראו `DELETIONS.md` לרשימה המלאה של מה זז לאן),
ואומתו לא רק ב-syntax check אלא בהרצה אמיתית דרך שני קובצי הבדיקה.

### החלטה מכוונת: i18n מקורי (`_locales/`) + השכבה המותאמת הקיימת — שתיהן, לא אחת במקום השנייה

STANDARDS.md §4/§8 דורש `_locales/` + `default_locale` + `__MSG_name__`
"לא מתג ידני custom". זה **לא** סתירה למערכת ה-i18n המותאמת הקיימת
(`ExtractifyI18n`) — זו שתי שכבות שפותרות בעיות שונות לגמרי:

- **`_locales/en|he/messages.json`** (חדש, גרסה זו) — פותר את המחרוזות
  **ברמת המניפסט** (`name`, `description`, טולטיפ הסמל בסרגל הכלים,
  תיאור הקיצור במסך `chrome://extensions/shortcuts`). אלה מוצגות על ידי
  **כרום עצמו**, מחוץ לכל עמוד שהתוסף שולט בו — אין דרך להריץ שם קוד
  JS מותאם בכלל, אז חייבים את המנגנון המובנה של כרום. כרום קובע איזו
  שפה מוצגת שם לפי `chrome.i18n.getUILanguage()` (שפת הדפדפן) עם נפילה
  ל-`default_locale` (עברית/אנגלית — הוגדר `"en"`).
- **`ExtractifyI18n`** (קיים מאז 1.2.0, לא הוסר) — פותר את המחרוזות
  **בתוך עמודים שהתוסף כן שולט בהם** (פאנל התוצאה, עמוד ההגדרות, עמוד
  הברוכים-הבאים). כאן יש דרישה ש-`_locales` המובנה **לא יכול** לספק:
  החלפת שפה **בזמן ריצה מתוך ממשק ההגדרות עצמו**, בלי תלות בשפת
  הדפדפן/המערכת בכלל — בדיוק ההחלטה המתועדת כבר ב-1.2.0 (ראו למטה
  בהיסטוריה). `chrome.i18n` נקבע פעם אחת בטעינת הדף ואין לו API להחלפה
  דינמית; מתג שפה בתוך Settings שהמשתמש רואה ולוחץ עליו **חייב** שכבה
  נפרדת.

בקיצור: המניפסט (מה שכרום מציג) עבר ל-`_locales` הרשמי; מה שבתוך
העמודים של התוסף עצמו נשאר על `ExtractifyI18n`. שתי השכבות מוזנות מאותו
טקסט בפועל (לא סתירה תוכן, רק מנגנון בחירה שונה) — עודכן גם `background.js`'s
`updateActionTitle()` שממשיך לדרוס את הטולטיפ בזמן ריצה לפי ההגדרה
שנבחרה ב-Settings, מעל ברירת המחדל שקובע `_locales`.

### תיעוד ומדיניות חדשים (STANDARDS.md §11.10, §11.11)

- **`PRIVACY.md`** — מדיניות פרטיות דו-לשונית. הנקודה המרכזית: Extractify
  לא שולח שום דבר לשום שרת (אפס בקשות רשת משל עצמו), אז דרישת הגילוי
  "Limited Use" של גוגל לעיבוד AI מרוחק (רלוונטית ל-ActionClip/
  TabToTable-AI ששולחים תוכן ל-Claude API) **לא חלה כאן בכלל** — אין
  עיבוד מרוחק לגלות עליו. זה חוזק אמיתי של הארכיטקטורה האופליין-לגמרי,
  לא רק "גם אנחנו עומדים בדרישה".
- **`EULA.md`** — רישיון שימוש קצר וברור (AS IS, ללא אחריות, שימוש
  אישי), דו-לשוני.
- **`store/STORE_LISTING.md`** — טקסט תיאור לחנות (עברית+אנגלית) והצדקת
  הרשאות, מוכן מראש גם שהתוסף לא מפורסם היום ב-Chrome Web Store (הפצה
  פרטית בקובץ ZIP). צילומי מסך/promo tile **נדחו במפורש** (מתועד בקובץ)
  — לא חוסמים דבר כל עוד לא מתפרסם בפועל.

### שם קובץ ה-ZIP

שונה מ-`Extractify.zip` הגנרי ל-`Extractify-v1.10.0.zip`, לפי המוסכמה
`<ExtName>-v<version>.zip` בסעיף 1. גם התוכן עצמו נוקה: ה-ZIP עכשיו
מכיל **רק** את מה שכרום צריך בפועל כדי לטעון את התוסף (`manifest.json`,
`src/`, `assets/`, `_locales/`) — לא מסמכי הפרויקט, לא כלי הבדיקה, לא
סקריפט יצירת האייקונים. 26 קבצים בסך הכול, `manifest.json` בשורש
הארכיון (אומת בפועל דרך פתיחת ה-ZIP ובדיקת רשימת הקבצים).

### בדיקות שבוצעו

- `manifest.json` ושני קובצי `_locales/*/messages.json` — JSON תקין.
- syntax check על כל 7 קובצי ה-JS מהמיקומים החדשים.
- סריקת אבטחה על כל הקוד שנכתב בפרויקט (לא על חבילת Tesseract הארוזה,
  צד-שלישי ולא-שלנו): אפס `eval`/`new Function`, אפס secrets, אפס
  `<script src="http...">`.
- שני קובצי הבדיקה הורצו בדפדפן אמיתי מול הנתיבים החדשים: `harness.html`
  (מנוע OCR אמיתי) — PASS; `panel-ui-harness.html` — הפאנל נטען, ה-grid
  מוצג נכון, והאייקון בכותרת הפאנל נטען בהצלחה מ-`assets/icons/icon32.png`
  החדש (לא רק "לא זרק שגיאה" — אומת ה-`src` בפועל מול ה-DOM).
- ה-ZIP הסופי נפתח ונבדק קובץ-קובץ (לא רק "הבנייה לא נכשלה").
- נסרק כל הפרויקט לוודא שאין עוד מקום שמחזיק `1.9.0` בקוד קשיח — מספר
  הגרסה נקרא במקום אחד יחיד (`chrome.runtime.getManifest().version`
  ב-`options.js`), אז אין סיכון לפיגור בין מקומות.

## גרסה 1.9.0 – דיוק, מהירות, ואפשרות חדשה

בעקבות בקשה מפורשת לשפר דיוק ומהירות ולהוסיף אפשרויות, לצד המשך בדיקת
תקינות. שלושה שינויים מהותיים, כל אחד אומת בנפרד מול המנוע האמיתי (לא
הונח כמובן מאליו) — וכן נמצא ותוקן באג concurrency אחד, שנוצר על ידי
אחד השיפורים עצמם, תוך כדי הבנייה, לפני שנשלח.

### שיפור דיוק: מתיחת ניגודיות (contrast stretch) — רק כשבאמת צריך

לפני OCR, האזור שצולם עובר עכשיו המרה לגווני אפור ומתיחת ניגודיות
ליניארית — **אבל רק כשהניגודיות בפועל נמוכה** (למשל טקסט אפור-בהיר
על רקע לבן בדשבורד, או טקסט מעל overlay שקוף-למחצה). המנגנון מודד את
טווח הבהירות (luminance) בפועל של האזור: אם הטווח כבר רחב (≥180 מתוך
255 — טקסט שחור רגיל על רקע לבן, המקרה הנפוץ), **שום דבר לא משתנה** —
נמנע לגמרי מהעיבוד. אם הטווח כמעט אפס (<8 — אזור כמעט אחיד), גם נמנע,
כדי לא להגביר רעש בלתי נראה. רק כשהטווח באמצע (8–180) — האזור המדויק
שבו Tesseract בדרך כלל מתקשה — מתבצעת המתיחה בפועל.

**החלטת עיצוב מרכזית**: זו לא הגדרה חדשה בהגדרות — זה תמיד פעיל, בלי
מתג. הסיבה: השומר (guard) שמדלג על אזורים בעלי ניגודיות טובה כבר
הופך את זה לבלתי-אפשרי שהשינוי "יזיק" לצילום רגיל — אין צורך להטיל
את ההחלטה הזו על המשתמש.

**איך אומת (לא רק תיאוריה)**: הורץ מבחן ישיר מול מנוע Tesseract
האמיתי (לא מדומה) עם שלושה תרחישים:
1. טקסט בניגודיות "עדינה" (`#f0f0f0`/`#c8c8c8`, טווח 40) — **בלי**
   המתיחה, הזיהוי טעה בפועל: "4217" זוהה כ-"77" (כישלון אמיתי, לא
   תיאורטי). **עם** המתיחה, זוהה נכון "4217 9080" בביטחון 95%.
2. טקסט בניגודיות "קיצונית" (`#f5f5f5`/`#dcdcdc`, טווח 25) — שני
   המקרים עברו זהה (Tesseract כבר טיפל בזה טוב); שום נסיגה.
3. צילום רגיל (שחור על לבן, טווח 255) — השומר דילג לגמרי; ה-Blob/
   ה-dataURL שנוצר **זהה בייט-לבייט** לפני ואחרי, מוכיח שאין שום
   השפעה על המקרה הנפוץ.

### שיפור מהירות: חימום מוקדם (pre-warm) של מנוע ה-OCR

טעינת Tesseract (WASM + נתוני שפה) לוקחת כ-1-2 שניות — הפעולה
הכי איטית בכל התהליך, ו**היא לא תלויה בצילום המסך בכלל**. עד עכשיו
היא התחילה רק **אחרי** שהמשתמש סיים לגרור בחירה. עכשיו, ברגע
שמתחילים צילום (לחיצה על הסמל, קיצור מקלדת, או תפריט ימני) —
`background.js` שולח הודעת `TABLIFY_WARM_ENGINE` ל-offscreen document
(ויוצר אותו אם צריך) **במקביל**, בלי לחכות. עד שהמשתמש מסיים לגרור
(בדרך כלל שנייה-שתיים של פעולה אנושית), המנוע כבר טעון או בדרך.
זו אופטימיזציה best-effort בלבד — אם הגרירה מהירה במיוחד או זה
הצילום הראשון אי-פעם, הצילום האמיתי עדיין יוצר את ה-worker בעצמו,
בדיוק כמו קודם.

### באג concurrency שנוצר על ידי השיפור עצמו, ותוקן לפני שנשלח

הוספת קריאת חימום יוצרת תרחיש חדש שלא היה קיים קודם: שתי קריאות
ל-`getWorker()` יכולות לקרות **כמעט בו-זמנית** — קריאת החימום, וקריאה
אמיתית מ-`TABLIFY_OFFSCREEN_RECOGNIZE` שמגיעה רגע אחרי. הקוד הישן
(`if (worker && ...) return worker; ... worker = await createOcrWorker(...)`)
לא היה בטוח מול race: שתי הקריאות היו רואות `worker === null` **שתיהן**,
ושתיהן היו מתחילות לטעון מנוע חדש — בזבוז טעינה שלמה, ודליפה שקטה של
ה-worker שהפסיד (ה-`worker = ...` השני פשוט דרס את הראשון, בלי שאף
אחד קרא לו `.terminate()`).

**התיקון**: תור (queue) מבוסס Promise-chaining — `workerQueue =
workerQueue.then(run, run)`. כל קריאה ל-`getWorker()` מחכה לקודמת
לפני שהיא רצה, כך שלעולם לא נוצרים שני workers בו-זמנית. חשוב: השרשור
דרך `(run, run)` (לא רק `.then(run)`) — כך שגם אם ניסיון קודם נכשל,
הניסיון הבא עדיין רץ במקום להישבר יחד איתו (Promise שנדחה לא מריץ
handler יחיד ב-`.then()` הבא בשרשרת אלמלא זה).

**איך אומת**: שוחזרה בדיוק הלוגיקה מול Tesseract האמיתי — שתי קריאות
מקבילות (`Promise.all`) עם אותן שפות: אומת ש-worker אחד בלבד נוצר
בפועל (`createCount === 1`) ושתי הקריאות קיבלו את **אותו מופע** worker
בדיוק. בנפרד, אומת שכשל מכוון בניסיון הראשון לא מנע מהניסיון השני
להצליח (`.then(run, run)` עובד כמתוכנן).

### אפשרות חדשה: העתקה אוטומטית

בהגדרות → סעיף חדש "Behavior" → תיבת סימון "Automatically copy the
result as a table when recognition finishes" (כבוי כברירת מחדל). כשדלוק,
הטבלה מועתקת ללוח **מיד** כשהזיהוי מסתיים — גם בצילום רגיל וגם אחרי
כל Add Capture — בלי צורך ללחוץ על "Copy as Table". מימוש: הקוד פשוט
מדמה לחיצה על כפתור ה-Copy as Table הקיים (`panel.el.querySelector(...).click()`)
במקום לשכפל את לוגיקת ההעתקה — כך שכל טיפול השגיאות/fallback שכבר
קיים שם (למשל דפים שחוסמים את ה-Clipboard API) עובד אוטומטית גם כאן.
נוספה שאלה חדשה ל-FAQ שמסבירה את ההתנהגות, למקרה שמישהו ידליק את
זה וישכח.

**איך אומת**: הופעל צילום אמיתי מול קוד `content.js` בפועל (לא רק
בדיקת לוגיקה מבודדת) עם `autoCopy: true` — אומת שה-clipboard קיבל את
תוכן הטבלה המדויק בלי שום לחיצה ידנית על כפתור העתקה.

### בדיקות נוספות שבוצעו

- "Add Capture" נבדק מחדש (regression) מעל כל השינויים למעלה — עדיין
  ממזג נכון לאותו מופע פאנל.
- syntax check על כל 7 קובצי ה-JS שנכתבו ידנית, ועל `manifest.json`.
- מילון התרגום: 145 מפתחות באנגלית = 145 בעברית (נוספו: `secBehaviorTitle`,
  `secBehaviorHint`, `autoCopyLabel`, `autoCopyHint`, `faqQ8`, `faqA8`).
- נסרקו כל הקבצים המקוריים לאיתור שאריות ניפוי או תיקוני-בדיקה זמניים
  שנשכחו — נמצא נקי. קובץ הבדיקה `test/panel-ui-harness.html` נעזר
  שוב זמנית ב-patch ל-`requestAnimationFrame` לצורך האימות, והוסר
  לפני שהקוד נארז.

## גרסה 1.8.1 – מעבר בדיקת תקינות ממוקד על 1.8.0

בעקבות בקשה מפורשת "תבדוק תקינות, תבצע התאמות שיפורים... מבלי להשתלט
על המחשב" — בוצעה קריאת קוד ידנית ומלאה על `content.js` בשלמותו (לא רק
על הקוד החדש), במיוחד סביב הלוגיקה העדינה של `STATE.appendMode` ושל
שיתוף הפאנל בין שני צילומים. **חשוב**: כל הבדיקות בוצעו דרך שרת פיתוח
מקומי (`python -m http.server`) ופיינל דפדפן מבודד (sandboxed) — לא
נעשה שימוש בכלי שליטה על שולחן העבודה האמיתי של המשתמש.

### באג 1: עריכה יכולה "להיבלע" בשקט ב-Add Capture

**התסמין**: משתמש שערך תא ברשת (או טקסט גולמי), ואז לחץ ישר על
"Add Capture" **בלי** לעבור קודם בין Grid ל-Text ובלי ללחוץ על אף
כפתור +/× — העריכה שלו **לא** הייתה מופיעה בטבלה הממוזגת. הצילום
החדש היה נוסף מעל הנתונים ה**ישנים** (לפני העריכה), לא מעל מה שבאמת
מוצג על המסך.

**הסיבה**: `appendResult()` פעל ישירות על המשתנה הפנימי `rows`, בלי
לסנכרן אותו קודם מה-DOM (בין אם מה-grid, בין אם מהטקסטאריה) — בדיוק
הסנכרון שפונקציית `currentTsv()` (שמזינה את כפתורי ההעתקה וההורדה)
**כן** מבצעת. שתי הפונקציות היו אמורות לשתף את אותה לוגיקת "קרא קודם
מהתצוגה הפעילה", ורק אחת מהן עשתה את זה בפועל.

**התיקון**: `appendResult()` עכשיו מבצע בדיוק את אותה בדיקה
(`if (mode === "grid") syncGridFromDom(); else rows =
parseRows(textarea.value);`) לפני המיזוג.

**איך אומת**: תא נערך ישירות ב-DOM (`cell.textContent = 'EDITED-Alice'`)
בלי לגעת בכפתור אחר, ואז הופעל Add Capture אמיתי עם גרירת עכבר אמיתית
מול קוד `content.js` שנארז בפועל — אומת שהתא הערוך מופיע בטבלה הסופית
בדיוק כפי שנערך.

### באג 2: קיצור מקלדת/תפריט ימני יכולים להרוס פאנל באמצע צילום

**התסמין**: אם משתמש היה באמצע session של "Add Capture" (צילום שני
עדיין מזוהה ברקע, לוקח כשנייה-שתיים), ומישהו הקיש `Ctrl+Shift+K` או
לחץ ימני → "Capture Area" **באותו זמן** — הבקשה החדשה הייתה מגיעה
ישירות מ-`background.js` דרך הודעת `SNAPTABLE_START`, שמפעילה
`beginSelection()` **בלי** דגל append (כי המקור החיצוני לא יודע שיש
צילום פעיל) — מה שסוגר את הפאנל הקיים תוך כדי שהצילום הראשון עדיין
מחכה לתשובה. כשהתשובה ההיא הגיעה בסוף, היא ניסתה לעדכן פאנל שכבר לא
קיים ב-DOM — התוצאה פשוט נעלמת בלי הודעת שגיאה.

**למה זה לא נתפס קודם**: כפתורי "New Area"/"Add Capture" **בתוך** הפאנל
כבר הושבתו (`disabled`) בזמן busy מאז 1.8.0 — אבל קיצור המקלדת והתפריט
הימני עוקפים את הפאנל לגמרי ופונים ישירות ל-`background.js`, כך שההגנה
הקיימת לא כיסתה אותם.

**התיקון**: נוסף דגל `STATE.busy` (true לאורך כל `captureAndRecognize`
ו-`recognizeDirectImage`, כולל בבלוק `finally`), ונבדק ב-listener
ההודעות הראשי לפני שמפעילים `beginSelection`/`recognizeDirectImage` —
אם busy, מוצג טוסט ("עדיין מזהה את הצילום הקודם") והבקשה מתעלמת, במקום
להרוס את הפאנל הקיים.

**איך אומת**: תגובת OCR מדומה הואטה בכוונה ל-800ms, ותוך כדי ההמתנה
הודעת `SNAPTABLE_START` הופעלה ישירות (מדמה קיצור מקלדת שמגיע דרך
background.js) — אומת: אותו מופע DOM בדיוק של הפאנל נשאר לאורך כל
הרצף, טוסט האזהרה הופיע עם הטקסט הנכון, והצילום המקורי המשיך והסתיים
כרגיל אחרי זה.

### בדיקות נוספות שבוצעו (לא רק על שני הבאגים)

- שרשרת ה-OCR **האמיתית** (לא מדומה) הורצה מחדש מקצה לקצה דרך
  `test/harness.html` נגד `tesseract.min.js`/`snaptable-ocr.js` הבלתי
  משתנים — PASS, ספרות נכונות, זיהוי עמודות נכון (~800ms) — מוודא
  שמנוע ה-OCR עצמו לא נפגע מהשינויים בסבב הזה.
- syntax check על כל 7 קובצי ה-JS שנכתבו ידנית (`background.js`,
  `content.js`, `offscreen.js`, `snaptable-ocr.js`, `extractify-i18n.js`,
  `options.js`, `welcome.js`).
- מילון התרגום: 139 מפתחות באנגלית = 139 בעברית (נוסף `errorBusy`).
- נסרקו כל הקבצים המקוריים (לא ספריות ממוזערות) לאיתור שאריות ניפוי
  (`console.log`) או תיקוני-בדיקה זמניים שנשכחו — נמצא נקי.
- קובץ הבדיקה הפנימי `test/panel-ui-harness.html` נעזר זמנית ב-patch
  ל-`requestAnimationFrame` (כי טאב הבדיקה האוטומטי מדווח
  `visibilityState: "hidden"` לצמיתות) — הוסר במפורש אחרי כל שימוש,
  לפני שהקוד נארז.

## גרסה 1.8.0 – מיזוג כמה צילומים לטבלה אחת ("Add Capture")

עד עכשיו כל צילום פתח פאנל תוצאה חדש שמחליף את הקודם. זה מספיק כשכל
הטבלה נראית על המסך בבת אחת — אבל **לא** כשהטבלה ארוכה מגובה המסך
(דשבורד ארוך, רשימה עם דפדוף/גלילה) — בדיוק התרחיש מהדיווח האמיתי על
`homey-family.vercel.app` שפתח את הפרויקט מלכתחילה. המשתמש היה צריך
לצלם כל "עמוד" בנפרד ולהדביק/למזג ידנית ב-Excel.

### כפתור חדש: "+ Add Capture"

בנוסף ל-"New Area" הקיים (שממשיך להתנהג בדיוק כמו קודם — מחליף את
הפאנל לגמרי), נוסף כפתור "+ Add Capture" שפותח בחירת-גרירה נוספת
**בלי לסגור את הפאנל הנוכחי** — וכשהתוצאה החדשה מגיעה, השורות שלה
**נוספות** לסוף הטבלה הקיימת (`rows.concat(newRows)`) במקום להחליף
אותה. שורת הסטטוס מדווחת "Added N more row(s) — M total now" כדי
שיהיה ברור מה קרה.

**החלטת עיצוב מרכזית**: הפאנל, ה-DOM, וה-closure שמחזיק את `rows`
נשארים **אותו מופע בדיוק** בין הצילומים — לא פאנל חדש עם מיזוג בדיעבד.
זה דורש חוט מקשר (`STATE.appendMode`) שעובר דרך `beginSelection()` →
`onMouseUp()` → `captureAndRecognize()`, כדי ש-`captureAndRecognize`
ידע **לא** לקרוא ל-`showPanel()` (שהיה הורס את המצב הקיים) אלא להשתמש
ב-`STATE.panelApi` שכבר קיים, ולקרוא למתודה חדשה `panel.appendResult()`
במקום `panel.setResult()`.

**באג עדין שנמצא ותוקן תוך כדי כתיבה (לא רק במחשבה)**: הנתיב "הבחירה
קטנה מדי, מתחילים מחדש" ב-`onMouseUp` (`if (w < 6 || h < 6)`) קרא
ל-`beginSelection()` **בלי** להעביר את דגל ה-append הנוכחי — כלומר אם
משתמש לחץ "Add Capture" ואז גרר בחירה קטנה מדי בטעות, הפאנל **היה
נסגר** על אף שהכוונה הייתה להוסיף אליו. תוקן להעביר `{ append:
STATE.appendMode }` גם בנתיב הזה.

### הגנה נוספת: לא ניתן להתחיל צילום חדש באמצע צילום פעיל

כפתורי "New Area" ו-"Add Capture" עכשיו מושבתים (`disabled`) בזמן
שצילום קודם עדיין מזוהה — סוגר פער עדין שהיה קיים גם **לפני** הגרסה
הזו (אפשר היה ללחוץ "New Area" פעמיים מהר ולגרום לשני בקשות async
במקביל) אבל הפך משמעותי יותר עכשיו: עם Add Capture, שתי בקשות
מקבילות **יכולות באמת** למקד את אותו פאנל בדיוק ולערבב סדר תוצאות.

### בדיקות שבוצעו

- שרשרת "Add Capture" מלאה הורצה מול **קוד `content.js` האמיתי
  שנארז** דרך אירועי עכבר אמיתיים (לא קריאה ישירה לפונקציה): צילום
  ראשון (3 שורות) → Add Capture → צילום שני (2 שורות) → אומת: אותו
  מופע DOM בדיוק של הפאנל, 5 שורות ב-grid, שורת הסטטוס וציון הביטחון
  משקפים את הצילום השני, ו-Copy as Table הפיק TSV נכון של כל 5 השורות
  (אומת מול אורך המחרוזת שהועתקה בפועל, לא רק "זה כנראה עובד").
- אומת ש-"New Area" **אחרי** מיזוג כזה עדיין מחליף את הפאנל לגמרי —
  מופע DOM חדש, אפס שורות שיורדו מהטבלה הקודמת. אין נסיגה (regression)
  בהתנהגות הקיימת.
- באג ה-"בחירה קטנה מדי" תואר למעלה נמצא בקריאת קוד תוך כדי כתיבת
  הבדיקה, ותוקן לפני שנארז.
- (הערת שיטת בדיקה: הדפדפן האוטומטי בסביבת הפיתוח מדווח לפעמים
  `document.visibilityState === "hidden"` לצמיתות על הטאב הנבדק, מה
  שמקפיא את ה-`requestAnimationFrame` הכפול ב-`captureAndRecognize`
  (המתנה לגיטימית לצייר מחדש בלי שכבת הבחירה). זו תופעה של כלי הבדיקה
  האוטומטי בלבד — בתוסף אמיתי הטאב תמיד גלוי בזמן שהמשתמש גורר בחירה
  — ולכן הבדיקה נעזרה זמנית ב-patch ל-`requestAnimationFrame` **בתוך
  קובץ הבדיקה `test/panel-ui-harness.html` בלבד**, שהוסר במפורש מיד
  אחרי האימות ולפני שהקוד נארז ל-ZIP.)

## גרסה 1.7.0 – טבלה אמיתית וניתנת לעריכה, הורדת CSV

עד עכשיו תוצאת ה-OCR הוצגה תמיד כטקסט גולמי (TSV) בתוך `<textarea>` —
מי שרצה לתקן טעות זיהוי בודדת (למשל "8" שזוהה כ-"B") היה צריך לספור
טאבים בעין ולערוך מחרוזת שטוחה. זה עבד, אבל לא ניצל את זה שהתוצאה
היא **טבלה אמיתית**, לא רק טקסט. זה השדרוג המשמעותי המרכזי בגרסה הזו.

### פאנל תוצאה: רשת (grid) ניתנת לעריכה, לא רק טקסט חופשי

התוצאה נפתחת עכשיו כטבלת HTML אמיתית עם תאים בני-עריכה
(`contenteditable`), עם כפתורי "+ שורה" / "+ עמודה", וכפתור "×" למחיקת
שורה/עמודה ספציפית. Enter בתוך תא **לא** מכניס שורה חדשה לתוך התא (זה
היה שובר את ה-TSV/CSV בשקט — תא לא יכול להכיל `\n`) — הוא מזיז את
הפוקוס לתא שמתחתיו, בדומה לגיליון אלקטרוני.

**מבנה הנתונים**: מערך של שורות, כל שורה מערך של תאים (`rows:
string[][]`) — זה מקור האמת היחיד. תצוגת ה-Grid ותצוגת ה-Text (הטקסטאריה
הישנה, עדיין קיימת) הן שתי דרכים שונות לערוך **אותם** הנתונים, לא שני
מצבים נפרדים: מתג "Grid / Text" ממיר בין השתיים בזמן החלפה
(`syncGridFromDom()` / `parseRows(textarea.value)`), כך שעריכה בכל אחת
מהתצוגות משתמרת גם אחרי מעבר לתצוגה השנייה ובחזרה.

**ברירת המחדל**: תוצאה עם טבלה שזוהתה (`result.isTable`) נפתחת ב-Grid;
טקסט חופשי (פסקה בלי מבנה עמודות ברור) נפתח ב-Text — בדיוק כמו
שהתנהגות ה-1.6.0 הייתה, רק שעכשיו יש גם אפשרות Grid לתוצאות טבלה.

### כפתור חדש: Download CSV

בנוסף לשלושת כפתורי ההעתקה הקיימים (Table/Text/Markdown), נוסף כפתור
שמוריד קובץ `.csv` אמיתי — לא רק מעתיק ללוח. חשוב: הקובץ נכתב עם
**UTF-8 BOM** (הבייטים `EF BB BF` בתחילת הקובץ) — בלעדיו, Excel מנחש
לפי ברירת מחדל קידוד אזורי (לרוב לא UTF-8) ומציג טקסט עברי כג'יבריש.
זו לא בעיה תיאורטית: התוסף מזהה עברית כשפת OCR ראשית, אז זה בדיוק
התרחיש הנפוץ. אומת ישירות מול בתי ה-bytes בפועל של ה-Blob שנוצר (לא
רק מול הטקסט המפוענח בחזרה, שבו הדפדפן **מסיר** את ה-BOM אוטומטית
בפענוח — בדיקה ראשונית שגויה כמעט הטעתה לחשוב שה-BOM חסר).

גם ה-CSV עצמו (לא רק ה-BOM) בונה ציטוט (quoting) נכון: כל תא שמכיל
פסיק, גרש כפול, או ירידת שורה עטוף במרכאות עם הכפלת גרשיים פנימיים —
כלל ה-CSV הסטנדרטי (RFC 4180), לא רק `join(",")` נאיבי.

### תיקון אגבי: הרתע הבדיקה הפנימית (`test/panel-ui-harness.html`)

תוך כדי שימוש בכלי הזה לבדיקת ה-grid, התגלה שהוא עדיין מדמה
`chrome.storage.sync` — שריד משכבר לפני המעבר ל-`chrome.storage.local`
ב-1.4.0. זה "עבד" עד עכשיו רק כי `getSettings()` בפועל קורא מ-
`chrome.storage.local`, שלא היה מדומה כלל, אז ההגדרות תמיד חזרו
ריקות/ברירת-מחדל — הבדיקה "עברה" במקרה, לא כי היא בדקה את המסלול
האמיתי. הקובץ הזה **לא** נארז ב-ZIP ולא משפיע על משתמשים, אבל תוקן
כדי שבדיקות עתידיות דרכו יהיו אמינות.

### בדיקות שבוצעו

- שרשרת מלאה הורצה בדפדפן אמיתי מול **קוד `content.js` האמיתי שנארז**
  (לא מוק): צילום מדומה → תוצאת טבלה → עריכת תא → הוספת שורה → הוספת
  עמודה → מחיקת עמודה ספציפית → מחיקת שורה ספציפית — כל שלב אומת מול
  ה-DOM בפועל, לא רק "זה כנראה עובד".
- מעבר Grid → Text → Grid עם עריכה בשתי הכיוונים אומת שהנתונים לא
  אבדו (כולל תא שנערך ב-Grid ואז שורה חדשה שנוספה דרך ה-Text).
- "Copy as Table" אומת מול `navigator.clipboard.writeText` (מדומה) —
  אורך המחרוזת שהועתקה תאם בדיוק לנתונים שבפועל היו ב-grid באותו רגע
  (כולל העריכות), לא לתמונת-מצב ישנה.
- "Download CSV": אומת ה-Blob שנוצר ברמת ה-**בתים** (`arrayBuffer()`),
  לא רק הטקסט המפוענח — ראה בייטי הפתיחה `ef bb bf` (UTF-8 BOM) ואז
  התוכן בפועל.
- תרחיש טקסט-לא-טבלה עם מחרוזת עברית אמיתית ("שלום עולם, זהו טקסט
  בעברית ללא טבלה.") אומת שנפתח כברירת מחדל במצב Text, עם ציון ביטחון
  מוצג נכון וטקסט מוצג/נבחר כראוי.
- כל 7 קובצי ה-JS שנגעו בהם עברו syntax check, ומילון התרגום אומת:
  135 מפתחות בעברית = 135 באנגלית (היו 127 ב-1.6.0; 8 מפתחות חדשים
  לפאנל ה-grid).

## גרסה 1.6.0 – באג אמיתי שנמצא ותוקן, ציון ביטחון AI, תאימות

בעקבות בקשה כללית נוספת שכללה במפורש "בדיקת תקינות וטיפול בבאגים" —
בוצעה הפעם **קריאת קוד ידנית וממוקדת** (לא רק syntax check) על כל קובצי
ה-JS העדכניים, במיוחד `background.js` ו-`offscreen/offscreen.js` שנוספו
ב-1.2.1 ולא עברו סקירה כזו עד עכשיו.

### באג אמיתי שנמצא: התקדמות (progress) יכולה לזלוג לטאב הלא-נכון

**התסמין** (לא היה מדווח, נמצא רק בקריאת קוד): מה-צילום ה-**שני** ואילך
בכל "חיי" ה-offscreen document (יכול להימשך זמן רב — הוא לא נסגר בין
צילומים), פס ההתקדמות בפאנל לא היה מתעדכן בזמן אמת (רק הטקסט "מזהה
טקסט…" הסטטי, בלי אחוזים חיים) — **אם** הצילום השני קרה בטאב **אחר**
מהראשון.

**הסיבה**: ה-worker של Tesseract נשמר ב-cache ומשמש שוב לצילומים
עוקבים (כדי לא לטעון מחדש WASM+נתוני שפה בכל פעם, ~1-2 שניות). אבל
Tesseract.js קושר את פונקציית ה-`logger` **פעם אחת בלבד**, בזמן יצירת
ה-worker (`createWorker(..., {logger})`) — אין `worker.setLogger()`. מכיוון
ש-`getWorker()` (ב-`offscreen.js`) העביר את פונקציית ה-progress **ישירות**
לתוך `createOcrWorker()`, היא נקבעה רק בפעם הראשונה שה-worker נוצר. בכל
קריאה עוקבת שבה ה-worker כבר קיים ב-cache (`if (worker && workerLangs
=== langKey) return worker`), הפונקציה החדשה (עם ה-`tabId` של הבקשה
הנוכחית) פשוט **לא הייתה מגיעה לשום מקום** — ה-logger הישן, עם ה-`tabId`
של הבקשה **הראשונה**, נשאר מחובר.

**חשוב**: התוצאה הסופית (הטקסט המזוהה עצמו) **לא** הייתה מושפעת — היא
מגיעה חזרה דרך שרשרת ה-Promise של `sendResponse`, לא לפי `tabId`. רק
עדכוני ה-**התקדמות החיה** (אחוזים, "טוען נתוני שפה…" וכו') היו יכולים
להישלח לטאב הלא-נכון — תקלה קוסמטית (פס התקדמות לא זז), לא תקלת נתונים.

**התיקון**: הוספת שכבת הפניה עקיפה — משתנה מודול `currentProgressHandler`
שנקבע **מחדש בכל בקשה** (לפני קריאה ל-`recognize()`), וה-logger שנקבע
פעם אחת ב-`createOcrWorker()` רק **מאציל** אליו (`(m) =>
currentProgressHandler?.(m)`). כך, גם כשה-worker עצמו נשמר ב-cache,
כל בקשה מקבלת ניתוב התקדמות נכון משלה.

**איך אומת (לא רק תוקן)**: נכתבה בדיקה שמדמה בדיוק את הלוגיקה של
`offscreen.js` (`getWorker`/`currentProgressHandler`) מול **קוד ה-OCR
האמיתי שנארז בתוסף** (לא מוק) — שתי "בקשות" עוקבות עם `tabId` שונה,
תוך שימוש **באותו worker cached**. אימות: בקשה 1 (`tab-AAA`) ראתה רק את
ה-`tabId` של עצמה בפינגים; בקשה 2 (`tab-BBB`), שחזרה על אותו worker
בדיוק, ראתה רק את `tab-BBB` — מוכיח שהבאג תוקן במדויק בתרחיש שגרם לו.

### חדש: ציון ביטחון AI (confidence)

Tesseract מחזיר תמיד ציון ביטחון ממוצע (0-100) לכל האזור שזוהה
(`data.confidence`, קיים תמיד, לא תלוי בדגל `blocks`). זו הייתה נתונה
שכבר קיים ולא נוצל — עכשיו הוא מוצג כ"צ'יפ" נוסף בפאנל (וגם בבדיקת
המנוע בהגדרות): "AI confidence: 92%". ציון מתחת ל-70% מודגש באדום —
איתות פשוט "כדאי לבדוק את זה לפני שסומכים עליו", בלי לנחש בשביל המשתמש.
זו בדיוק סוג השקיפות שמתאימה למוצר שמתאר את עצמו כ"AI מקומי" — לא רק
מילת באזז, אלא הבהרה כנה שהמודל (רשת נוירונים) *לא* בטוח ב-100% תמיד.

### תאימות: `minimum_chrome_version`

נוסף `"minimum_chrome_version": "116"` ל-manifest. הסיבה: התוסף תלוי
ב-`chrome.runtime.getContexts()` (נוסף ב-Chrome 116, משמש ב-
`ensureOffscreenDocument` כדי לבדוק אם ה-offscreen document כבר קיים
לפני יצירתו מחדש). בלי ההצהרה הזו, משתמש עם כרום ישן מדי היה מקבל
כשל זמן-ריצה עמום (למשל "getContexts is not a function"); עם ההצהרה,
כרום עצמו מציג הודעה ברורה שהתוסף לא נתמך בגרסה הזו, לפני שהוא בכלל
מנסה לטעון אותו.

### שיפור עמידות קטן: מיקום פאנל זכור מוגבל לגבולות המסך

`STATE.lastPanelPos` (מיקום הפאנל שנגרר, נזכר לצילומים הבאים באותו
עמוד) עכשיו "נצבט" (clamp) לגבולות ה-viewport הנוכחי בכל פתיחה מחדש —
מונע מצב שבו גרירת הפאנל לפני שינוי גודל חלון (או גלילה בעמוד ארוך)
תגרום לו להיפתח חלקית/לגמרי מחוץ למסך בלי דרך גלויה לגרור אותו בחזרה.

### בדיקות שבוצעו
- קריאת קוד ידנית מלאה על `background.js`, `content.js`, `offscreen.js`,
  `snaptable-ocr.js` — לא רק syntax check.
- מילון התרגום נבדק: 127 מפתחות בעברית = 127 באנגלית.
- שרשרת ה-OCR המלאה הורצה מחדש דרך `test/harness.html` (PASS, 430ms).
- ציון הביטחון אומת כמספר תקין (96 על טקסט "HELLO" נקי) מול קוד ה-OCR
  האמיתי, לא מדומה.
- באג ניתוב ה-progress אומת ותוקן כמתואר למעלה — בדיקה ממוקדת שכתבתי
  במיוחד כדי לשחזר את התנאים שגרמו לו (worker cached, שתי בקשות עם
  tabId שונה).

## גרסה 1.5.0 – הוסר: קובץ ה-EXE/‏`.bat` להתקנה

בעקבות משוב ישיר: **"לא צריך קובץ התקנה EXE לתוסף אם זה תוסף של כרום"**.
המשוב מדויק, ומצדיק הפיכת החלטה קודמת (ראו הסעיף "תוספת: קובץ התקנה
גרפי" למטה, שנשאר כתיעוד היסטורי של מה שנבנה ולמה — לא נמחק, כי ה"למה
ננסה וגם למה ביטלנו" הוא בדיוק מה ש-SPEC.md אמור לתעד).

**מה הוסר בפועל:**
- `Extractify-Setup.exe` (78MB) ותיקיית `installer/` כולה — פרויקט
  .NET WinForms שהפיק אותו.
- `Install-Extractify.bat` ו-`Uninstall-Extractify.bat`.

**למה זה היה שגוי מלכתחילה (לא רק "משוב טעם")**: תוסף Chrome, מטבעו,
**לא** מותקן כמו תוכנה רגילה (שצריכה קובץ EXE שמעתיק DLL-ים, כותב
לרג'יסטרי וכו'). הדרך התקנית והיחידה לטעון תוסף לא-ארוז היא "Load
unpacked" מתוך `chrome://extensions` — כרום עצמו קורא ישירות מהתיקייה,
בלי "התקנה" בכלל במובן המקובל. בניית אשף EXE שמעתיק קבצים ל-
`%LOCALAPPDATA%` ואז מפעיל כרום עם `--load-extension` **פתרה בעיה
שלא הייתה קיימת** (איך "להתקין" תוסף) במחיר של מורכבות אמיתית: פרויקט
.NET נפרד לתחזק, 78MB נוספים, וקובץ בלתי-חתום שמעורר אזהרת SmartScreen.

**תופעת לוואי שהתגלתה בבדיקה החוזרת**: תיקיית `installer/` הכילה עותק
שני שלם של `extension/` דחוס (`installer/ExtractifyInstaller/Resources/
extension.zip`, ~6MB) שהיה צריך להתעדכן ידנית בכל build — כפילות נתונים
אמיתית, לא רק תיאורטית, שנעלמת אוטומטית עם הסרת התיקייה.

**מבנה ה-ZIP חזר לשטוח**: `Extractify.zip` מכיל את `extension/*` ישירות
בשורש (לא בתוך תת-תיקייה עם סקריפטים לצידה) — `manifest.json` נמצא
ישירות בשורש הארכיון, בדיוק כמו רוב תוספי הכרום שמופצים כ-ZIP.

### בדיקה שבוצעה
- `python -c "zipfile..."` אימת בפועל ש-`manifest.json` נמצא בשורש
  הארכיון (לא בתוך תת-תיקייה) ושה-ZIP מכיל 24 קבצים — בדיוק תוכן
  `extension/`, שום דבר נוסף.
- חיפוש hash (`md5sum`) על כל קובץ בפרויקט אימת שאין שני קבצים בעלי
  תוכן זהה בשום מקום בפרויקט (מעבר לכפילות ה-ZIP-בתוך-EXE שכבר תוארה
  והוסרה).
- התוסף המותקן בפועל (`%LOCALAPPDATA%\Tablify\extension`) עודכן לגרסה
  1.5.0 ונבדק שהוא עדיין תואם למקור (`manifest.json` מציג את השם
  והגרסה הנכונים).

## גרסה 1.4.0 – אבטחה, פרטיות והבהרות

בעקבות בקשה כללית נוספת לשדרוגים, כולל דגש מפורש על "רשמיות, אבטחה,
נתונים, הסברים שהכול יהיה ברור, ובינה מלאכותית":

### שינוי אמיתי (לא רק תיעוד): הגדרות עברו מ-`chrome.storage.sync` ל-`chrome.storage.local`

זו לא הייתה רק "הבהרה" – זה שינוי קוד אמיתי. `chrome.storage.sync`
**מסתנכרן דרך שרתי Google** (אם המשתמש מחובר לחשבון גוגל בכרום ו-Chrome
Sync דלוק) — כלומר עד עכשיו, ההגדרות (שפת ממשק, שפות OCR, רגישות עמודות)
**כן** יכלו לגעת בשרת חיצוני, בסתירה עדינה לטענת השיווק "הכול רץ אופליין,
אין שרת". התוכן הרגיש (צילומי מסך, טקסט מזוהה) מעולם לא היה חלק מזה –
הוא תמיד נשאר בזיכרון בלבד ולא נשמר בשום `chrome.storage` – אבל ההגדרות
עצמן כן היו חשופות לזרימת הנתונים הזו, גם אם לא רגישות במיוחד. **התיקון**:
כל שימוש הוחלף ל-`chrome.storage.local` (רק על המכשיר, לעולם לא מסונכרן).
תופעת לוואי חד-פעמית: הגדרות ששמורות מגרסה קודמת ב-`sync` **לא** יעברו
אוטומטית ל-`local` (זה storage area נפרד לגמרי בכרום) – המשתמשים יחזרו
לברירת המחדל פעם אחת. מקובל, כי מדובר בשלוש הגדרות לא-קריטיות.

### עמוד הגדרות חדש: "אבטחה ופרטיות"

נוסף כרטיס ייעודי בעמוד ההגדרות שמסביר בשפה פשוטה (לא ז'רגון משפטי):
1. מה קורה עם הנתונים בפועל (אחרי התיקון למעלה – הכול נשאר במכשיר, תמיד).
2. **רשימת כל הרשאה שה-manifest מבקש, ולמה** (`activeTab`, `scripting`,
   `storage`, `clipboardWrite`, `contextMenus`, `offscreen`, גישה לכל
   האתרים) — בתוך `<details>` מתקפל, כדי שהעמוד יישאר קומפקטי כברירת
   מחדל אך המידע יהיה נגיש למי שרוצה. ההרשאה הכי "מפחידה" מבחינת משתמש
   (גישה לכל האתרים) קיבלה את ההסבר הכי מפורט ("הרשאה רחבה מטבעה, אבל
   שום דבר לא נשלח החוצה בעקבותיה").
3. שאלה נוספת ב-FAQ שמקשרת חזרה לנושא (`offscreen`, CSP) בשפה פשוטה יותר
   מזו שב-SPEC.md.

### מיצוב "AI" — מדויק, לא רק שיווקי

נוספה התייחסות ל-Extractify כמשתמש ב"AI מקומי" (ב-manifest, במסך
"ברוכים הבאים", ובכרטיס האבטחה). זו טענה **מדויקת ולא מוגזמת**: מנוע
ה-OCR של Tesseract (מגרסה 4 ואילך, שבה נעשה שימוש כאן — `oem=1`,
LSTM_ONLY) מבוסס רשת נוירונים חוזרת (RNN/LSTM) לזיהוי תווים — זו בהחלט
"בינה מלאכותית" במובן הטכני המדויק, לא רק מילת באזז. חשוב באותה מידה מה
**לא** נאמר: אין כאן "AI בענן" (כמו ChatGPT/Claude) שמנקה או "משפר" את
הטקסט המזוהה — זה נשאר תחום עתידי אפשרי (ראו "המשך אפשרי"), שנשקל
במפורש ונדחה כרגע כי זה ידרוש חיבור לאינטרנט/מפתח API/עלות, בסתירה
לעמדת הפרטיות המרכזית של המוצר.

### בדיקות שבוצעו
- כל קובצי ה-JS נבדקו syntax (`node --check`) אחרי המעבר ל-`storage.local`.
- **נמצא ותוקן תוך כדי בדיקה**: מאזין `chrome.storage.onChanged` ב-
  `background.js` עדיין בדק `area === "sync"` (מחרוזת קשיחה) אחרי שהקריאות
  עצמן כבר הוחלפו ל-`local` — התיקון האוטומטי (`sed`) של הקריאות לא נגע
  במחרוזת ההשוואה הנפרדת הזו. בלי התיקון, שינוי הגדרות לא היה מרענן את
  התפריט בלחיצה ימנית/טולטיפ הסמל בזמן אמת (רק אחרי Reload ידני). זה
  בדיוק סוג הבאג ש"בדיקה חזותית" לא הייתה תופסת (אין לו ביטוי ב-UI מיידי)
  — נמצא רק בקריאה ידנית וקפדנית של כל שימוש ב-storage.
- מילון התרגום נבדק פרוגרמטית: 126 מפתחות בעברית = 126 באנגלית, זהים
  לחלוטין (אין חסר בשום צד).
- עמוד ההגדרות המלא (כולל הכרטיס החדש, עם ה-`<details>` פתוח) הורץ
  בדפדפן אמיתי (עם "לחשן" ל-`chrome.storage.local`) ונקרא בחזרה **גם
  באנגלית וגם בעברית** — כל הטקסט אומת מילה-במילה, כולל באנר "עודכן
  לגרסה" (מוודא ש-`chrome.storage.local` עובד נכון גם להתראת עדכון).
- בדיקת מנוע ה-OCR ("הרצת בדיקה") הורצה מחדש אחרי כל השינויים — עברה
  ✅ (349ms, זיהוי מדויק, 2 עמודות) — מוודא ששינוי ה-storage לא שבר בטעות
  את שרשרת ה-OCR (הן לא קשורות ישירות בקוד, אבל זו בדיקת שפיות מהירה).

## גרסה 1.3.1 – שיפורי איכות זיהוי וחוויית שימוש

בעקבות בקשה כללית לשדרג עוד את החוויה, ללא כיוון ספציפי:

- **הגדלה אוטומטית של צילומים קטנים לפני OCR**: תיעוד Tesseract.js עצמו
  ממליץ על רזולוציה גבוהה יותר לדיוק זיהוי טוב יותר. אזורים קטנים (למשל
  תווית באמצע דשבורד) שהצד הארוך שלהם קטן מ-700px מוגדלים עכשיו פי עד 3
  (עם `imageSmoothingQuality: "high"`) לפני שליחה ל-OCR — צילומים גדולים
  מספיק נשארים בדיוק כמו שהיו (`scale=1`, ללא שינוי התנהגות).
- **כפתור "Copy as Markdown" חדש**: ליד "העתק כטבלה" ו"העתק כטקסט". אם
  זוהה מבנה עמודות (יש Tab בטקסט) — הופך לטבלת Markdown תקנית (עם בריחת
  תווי `|`); אם לא — עוטף את הטקסט בבלוק קוד Markdown (` ``` `), כדי
  שהתוצאה תמיד תהיה תקינה ושימושית להדבקה ב-Notion/GitHub/מסמכים, לא רק
  ב-Excel.
- **באג אמיתי שנמצא ותוקן: Esc לא סגר את הפאנל**. מאזין המקלדת הותקן
  ופורק (`addEventListener`/`removeEventListener`) בכל פעם שמצב הבחירה
  נפתח/נסגר — אבל הפאנל נפתח **אחרי** ש-`cleanupSelectionOverlay()` כבר
  פירק את המאזין, כך שברגע שהפאנל היה פתוח, Esc לא עשה כלום. תוקן ע"י
  הצמדת מאזין מקלדת **אחד גלובלי**, לכל אורך חיי העמוד (לא per-selection),
  שבודק את מצב ה-state הנוכחי בזמן ריצה במקום להיות תלוי במתי הוא צורף.
- **קיצור מקלדת חדש: Ctrl+Enter / ⌘+Enter** בזמן שהפאנל פתוח = "העתק
  כטבלה" מיידי, בלי לגעת בעכבר.
- **מיקום הפאנל נשמר** בין צילומים באותו טאב (אם גררתם אותו למקום נוח,
  הצילום הבא ייפתח שם, לא יחזור לפינה המוגדרת כברירת מחדל).

### בדיקות שבוצעו
נבנה כלי בדיקה נוסף (`test/panel-ui-harness.html`, נשאר בפרויקט לשימוש
עתידי — לא נארז ב-ZIP) שמריץ את `content.js` האמיתי בדפדפן עם "לחשן" ל-
`chrome.*` (כולל תגובה מדומה ל-OCR, כדי לבדוק את ה-UI במהירות בלי לחכות
לזיהוי אמיתי) ומדמה גרירת-בחירה אמיתית. **אומת בפועל בדפדפן**: הפאנל
נפתח עם כל 4 הכפתורים (כולל Markdown) על שורה אחת בלי גלישה/חיתוך;
Ctrl+Enter מפעיל `clipboard.writeText` בפועל; Escape סוגר את הפאנל
(מקודם לא עבד בכלל — עכשיו כן). לוגיקת ההגדלה האוטומטית וזכירת המיקום
נבדקו בקריאת קוד בלבד (שינוי קטן, מבוסס ישירות על לוגיקת הגרירה
הקיימת שכבר אומתה בעבר) ולא הורצו ידנית בנפרד.

## גרסה 1.3.0 – שינוי שם: Tablify ← Extractify

**למה עוד שינוי שם?** "Tablify" (מ-Table) רמז שהתוסף מיועד רק לטבלאות.
בפועל הוא מחלץ טקסט מכל תמונה או PDF שמוצג בדפדפן — טבלה, פסקת טקסט,
תעודה סרוקה, כל דבר. **Extractify** (מ-Extract) משקף את זה נכון יותר,
ושומר על אותה סיומת "-ify" ממותגת מהשם הקודם.

**מה השתנה בפועל**: כל טקסט הפונה למשתמש (manifest, עמוד הגדרות, פאנל,
מסך "ברוכים הבאים", תפריט לחיצה ימנית, קובץ ההתקנה) עודכן ל-Extractify.
שמות קבצים/תיקיות שהמשתמש עלול לראות שונו גם הם: `lib/tablify-i18n.js`
→ `lib/extractify-i18n.js`, `Tablify-Setup.exe` → `Extractify-Setup.exe`,
`Install-Tablify.bat`/`Uninstall-Tablify.bat` →
`Install-Extractify.bat`/`Uninstall-Extractify.bat`, תיקיית פרויקט
`ExtractifyInstaller` (במקום `TablifyInstaller`), `extractify.ico`.
זהות עיצובית (לוגו, צבעים) **לא** השתנתה — היא מושגית ומתאימה לכל שם.

**מה *לא* שונה, ולמה**: כמה מזהים פנימיים לגמרי (בלתי-נראים למשתמש)
נשארו בשמם המקורי כדי לצמצם סיכון בלי שום תועלת בפועל: שם הקובץ
`lib/snaptable-ocr.js`, המשתנה הגלובלי `SnapTableOCR`, סוגי ההודעות
הפנימיים בין חלקי התוסף (`SNAPTABLE_START` וכו') ומחלקות ה-CSS
(`snaptable-*`) — כולם שרידים היסטוריים מהשם הראשון (SnapTable OCR)
שכבר לא שונו גם בשינוי השם הקודם (ל-Tablify), ואין להם שום השפעה
משתמש-נראית.

**מגבלה שהתגלתה בתהליך**: לא ניתן לשנות את שם **תיקיית הפרויקט** עצמה
(`CLAUDE BOTS\Tablify` בדיסק) בזמן שהסשן הזה פעיל — Windows חוסם שינוי
שם לתיקייה שתהליך מחזיק אותה כ-working directory פתוח, וזה בדיוק המצב
כאן (הסשן של Claude Code עצמו משתמש בתיקייה הזו כתיקיית הפרויקט הפעילה).
זו מגבלת מערכת הפעלה, לא קשורה לתוסף — ניתן לשנות את שם התיקייה ידנית
מאוחר יותר (למשל מ-File Explorer, אחרי סגירת הסשן) בלי שום השפעה על
התוסף עצמו (הוא מותקן בנפרד ב-`%LOCALAPPDATA%`, לא תלוי בשם תיקיית
המקור). **התיקייה המותקנת בפועל אצלך** (`%LOCALAPPDATA%\Tablify\extension`)
גם היא נשארה באותו נתיב פיזי בכוונה — שינוי הנתיב היה דורש התקנה מחדש
מלאה (Load unpacked מחדש), מזהה תוסף (Extension ID) חדש, ואיפוס כל
ההגדרות השמורות (`chrome.storage.sync`). זה בלתי-נראה בפועל: כרום מציג
את השם מה-manifest, לא משם התיקייה, כך שהתוסף מוצג כ"Extractify" תקין
לגמרי גם כשהקבצים פיזית יושבים בתיקייה שעדיין נקראת "Tablify".

## גרסה 1.2.1 – תיקון: OCR נכשל באתרים עם CSP מחמיר

**באג אמיתי שדווח ע"י המשתמש**, עם הודעת שגיאה מדויקת מהאתר
`homey-family.vercel.app`:
```
Failed to construct 'Worker': Script at 'chrome-extension://.../lib/worker.min.js'
cannot be accessed from origin 'https://homey-family.vercel.app'.
```

**הסיבה**: הנחת יסוד שגויה שהופיעה במסמך הזה עד עכשיו — "ה-worker של
Tesseract.js נטען מ-chrome-extension:// ולכן פועל תחת ה-CSP של התוסף,
לא של האתר המארח". זה **נכון רק לרוב האתרים** (שאין להם CSP מחמיר), אבל
לא תמיד: Worker שנוצר מתוך content script (`new Worker(...)`) עדיין כפוף
ל-CSP (`worker-src`/`script-src`) של **הדף המארח** עצמו, כי הדפדפן קושר
את הקשר האבטחה של ה-Worker למסמך שיצר אותו — לא למקור קובץ ה-JS. אתרים
עם CSP שמגביל `worker-src` (נפוץ באתרי Vercel/Next.js מודרניים עם מדיניות
אבטחה קפדנית) חוסמים את זה לגמרי.

**התיקון**: הועבר כל ריצת ה-OCR (יצירת ה-Worker של Tesseract, זיהוי,
שחזור טבלה) ל-**Offscreen Document** — עמוד תוסף נסתר ייעודי
(`offscreen/offscreen.html`), שהוא הפתרון הרשמי והמתועד של Chrome
בדיוק לבעיה הזו (Manifest V3, permission `"offscreen"`). עמוד כזה פועל
תמיד תחת ה-CSP **של התוסף עצמו** בלבד, בלי שום תלות בדף שממנו בוצע
הצילום.

**ארכיטקטורה חדשה**:
```
content.js (בדף המקורי)          background.js               offscreen.js
  - צילום/חיתוך/UI בלבד     →    - מוודא ש-offscreen        - כל לוגיקת
  - שולח TABLIFY_RECOGNIZE_        document קיים               ה-OCR עברה
    REQUEST                   →  - מעביר הלאה ל-offscreen      לכאן
  - מקבל תוצאה סופית         ←   - מעביר תוצאה/התקדמות      ← Tesseract
    (או הודעות התקדמות            בחזרה                        Worker נוצר
    SNAPTABLE_PROGRESS)                                          כאן, לא בדף
```

תוצאה נוספת נחמדה: `content.js` כבר לא צריך לטעון את `tesseract.min.js`
או `snaptable-ocr.js` לתוך הדף המארח בכלל (רק `extractify-i18n.js` +
עצמו) — כלומר גם באתרים "רגילים" ללא CSP מחמיר, פחות קוד רץ בתוך
ה-context של הדף עצמו.

**מה כן נבדק**: כל הקבצים נבדקו syntax (`node --check`), ומבנה ה-API
(`chrome.offscreen.createDocument`, `chrome.runtime.getContexts`) נבדק
מול תיעוד Chrome הרשמי. **מה שלא נבדק אינטראקטיבית**: לא הצלחתי לשחזר
בעצמי אתר עם CSP מחמיר כזה כדי לאשר את התיקון בפועל באותם תנאים בדיוק
(הבדיקה שביצעתי קודם, ב-`test/harness.html`, מדמה דף רגיל בלי CSP קפדני
— כלומר לא הייתה יכולה לתפוס את הבאג הזה מלכתחילה). התיקון מבוסס על
ההבנה הנכונה של הבעיה + הפתרון הרשמי המתועד של Chrome, לא על שחזור
מקומי. **מומלץ שתבדוק בעצמך** שוב באותו אתר (homey-family.vercel.app)
אחרי לחיצה על ↻ Reload ב-`chrome://extensions`.

## תוספת: קובץ התקנה גרפי (Extractify-Setup.exe) — **הוסר בגרסה 1.5.0**

> ⚠️ **הסעיף הזה מתאר תכונה שהוסרה.** נשאר כתיעוד היסטורי (מה נבנה,
> ולמה) — ראו "גרסה 1.5.0" למעלה למה זה בוטל. אל תסתמכו על קבצים
> שמוזכרים כאן (`installer/`, `Extractify-Setup.exe`) — הם לא קיימים
> יותר בפרויקט.

בעקבות בקשה נוספת ("קובץ התקנה מעוצב יפה של EXE, יותר קל ונוח למשתמשים"),
נוסף אשף התקנה גרפי אמיתי (חלונות Next/Install/Finish, פס התקדמות,
מיתוג תואם) — לא רק סקריפט `.bat`.

### איך זה נבנה (ובלי להוריד כלום חדש)
נבדק שהמחשב כבר כולל **.NET 8 SDK** ואת קומפיילר ה-C# (`csc.exe`, חלק
מ-.NET Framework המובנה בוינדוס) — כך שנבנה אשף **Windows Forms** מלא
ב-C#, ופורסם כ-**קובץ EXE יחיד, self-contained** (כולל את כל ה-runtime
של .NET~בפנים, כך שהוא רץ על כל מחשב Windows גם בלי .NET מותקן), בלי
להוריד או להתקין שום כלי חיצוני (לא Inno Setup, לא NSIS). כל תיקיית
`extension` נדחסת ל-ZIP ומוטמעת (`EmbeddedResource`) בתוך ה-EXE עצמו —
כך שזה **קובץ יחיד אמיתי** (78MB, כולל ה-runtime + כל קבצי ה-OCR), לא
EXE שדורש תיקיית "extension" לידו.

### מה האשף עושה
1. **Welcome** – הסבר קצר על התוסף.
2. **Options** – בחירת תיקיית התקנה (ברירת מחדל `%LOCALAPPDATA%\Extractify`),
   ותיבות סימון: "פתח את כרום עם Extractify אחרי ההתקנה" ו-"צור קיצור בשולחן
   העבודה" (שניהם מסומנים כברירת מחדל).
3. **Progress** – פס התקדמות אמיתי תוך כדי חילוץ קבצי ה-ZIP המוטמע.
4. **Finish** – מסכם מה קרה: איפה הותקן, האם קיצור נוצר, והאם כרום נפתח.
   **חשוב**: אם כרום כבר היה פתוח בזמן ההתקנה, האשף **לא** מנסה לכפות
   טעינה (כרום מתעלם מהדגל `--load-extension` אם התהליך כבר רץ) — הוא
   מזהה זאת (`Process.GetProcessesByName("chrome")`) ומציג הודעה ברורה
   שמסבירה למשתמש לסגור את כרום ולהשתמש בקיצור, בלי לנסות "לכפות" סגירה
   של הדפדפן (פעולה הרסנית פוטנציאלית שנמנעה ממנה בכוונה — בדיוק כמו
   ב-`Install-Extractify.bat`).

### פרטי מימוש שכדאי לדעת
- **קיצור שולחן העבודה** נוצר באמצעות אוטומציית COM מסוג `WScript.Shell`,
  שנקשרת בזמן ריצה (reflection, `Type.GetTypeFromProgID` +
  `InvokeMember`) ולא דרך מילת המפתח `dynamic` של C# — כי `dynamic` היה
  דורש הוספת חבילת NuGet (`Microsoft.CSharp`) שדורשת גישה לאינטרנט
  בזמן build; הגישה שנבחרה עובדת "אופליין" לגמרי.
- **אייקון ה-EXE** (`extractify.ico`) נבנה פרוגרמטית מקבצי ה-PNG הקיימים
  (16/32/48/128) על ידי ארגז-כלים קטן (`installer/make-ico.ps1`) שבונה
  את מבנה ה-ICO הבינארי (container "PNG-in-ICO", נתמך מ-Windows Vista
  ואילך) ישירות — בלי כלי המרה חיצוני.
- **הרשאות**: מוגדר עם `requestedExecutionLevel level="asInvoker"`
  (ב-`app.manifest`) — לא דורש הרשאות מנהל, כי ההתקנה היא ל-
  `%LOCALAPPDATA%` (תיקיית המשתמש), לא ל-`Program Files`.
- **אין חתימה דיגיטלית (code signing)**: זה אומר שכרום/וינדוס עשויים
  להראות אזהרת "מפרסם לא ידוע" (SmartScreen) בהרצה ראשונה — זה נורמלי
  לכל exe שלא נרכשה לו תעודת חתימה (עולה כסף, לא נחוץ לשימוש פרטי/פנימי).
  אפשר ללחוץ "עוד מידע" → "הפעל בכל זאת".

### בדיקה שבוצעה (ובאג אמיתי שנמצא ותוקן)
האשף הורץ בפועל (לא רק נבדק קומפילציה) על המחשב, כולל מעבר בין המסכים.
**נמצא באג אמיתי**: לחיצה על "Next" בעמוד הראשון לפעמים "דילגה" ישר עד
"Finish", מבלי לעצור בעמודי Options/Progress. הסיבה: `RunInstall()`
קורא ל-`Application.DoEvents()` תוך כדי חילוץ ה-ZIP (כדי שפס ההתקדמות
יצטייר), ואם לחיצה נוספת כבר הייתה בתור ההודעות של Windows באותו רגע
(למשל לחיצה מהירה פעמיים), `DoEvents()` היה "מעבד" אותה מוקדם מדי,
וגורם לאירוע Click להיכנס שוב לפונקציה (**reentrancy**) באמצע טיפול
בלחיצה הקודמת — מה שקפץ בין מסכים בצורה לא צפויה. **התיקון**: השבתת
כפתורי Next/Back/Cancel (`Enabled = false`) לכל משך זמן הטיפול בלחיצה —
כפתור מושבת לא מייצר אירוע Click בכלל, כך שהבעיה נמנעת מבנית (Win32
guarantee), ולא משנה מה גרם ללחיצה הכפולה מלכתחילה.
לאחר התיקון והידור מחדש, עמוד הפתיחה (Welcome) אומת חזותית שוב שהוא
עדיין נראה תקין (מיתוג, גרדיאנט, טקסט). **לא הושלמה** אימות חזותי מלא
של כל 4 העמודים ברצף לאחר התיקון — סביבת הבדיקה (מחשב עם הרבה חלונות
ופרויקטים אחרים פועלים בו-זמנית) גרמה לחוסר יציבות בפוקוס החלון בין
קריאות הכלים, ולא רציתי להמשיך "לדחוף" בדיקות אינטראקטיביות על מחשב
עמוס בפעילות לא-קשורה. **מה כן נבדק ואומת בפועל**: הלוגיקה המרכזית
(חילוץ קבצים ל-`%LOCALAPPDATA%\Extractify\extension`, יצירת קיצור בשולחן
העבודה, זיהוי נכון של כרום פתוח והצגת הודעה מתאימה במקום ניסיון כושל
לטעון) **כן** רצה בפועל בהצלחה בהרצה מלאה אחת (לפני התיקון, אך הקוד
הזה לא נגע בו כלל). מומלץ שתריץ את ה-EXE בעצמך פעם אחת כבדיקת קבלה.

## גרסה 1.2.0 – מיתוג מחדש, דו-לשוניות, ולחיצה ימנית

### מיתוג מחדש: Extractify
שם המוצר, האייקון (נשאר עיצובית זהה – עדשת צילום + רשת טבלה, רלוונטי לכל
שם), עמוד ההגדרות, הפאנל, וכל טקסט הפונה למשתמש – כולם עברו לשם **Extractify**
ולברירת מחדל **אנגלית** (לפי בקשה מפורשת), עם אפשרות מלאה למעבר לעברית.

### ממשק דו-לשוני (אנגלית ברירת מחדל, עברית זמינה)
נוסף מודול תרגום ייעודי (`lib/extractify-i18n.js`) – לא `chrome.i18n` הרגיל
של כרום, כי הוא נקבע ע"פ שפת המערכת של הדפדפן ולא ניתן להחלפה מתוך התוסף
עצמו; כאן המשתמש בוחר בעצמו מתוך עמוד ההגדרות (כפתורי EN / עב), וההעדפה
נשמרת ומשפיעה גם על פאנל הצילום וגם על הכיוון (LTR/RTL) של כל הממשק, מיידית
וללא רענון דף. **מגבלה שכדאי לדעת**: שם התוסף כפי שהוא מופיע ברשימת
`chrome://extensions` ובטולטיפ הבסיסי קבוע ל-manifest ולא ניתן להחלפה
דינמית ע"י כרום עצמו (זו מגבלת הפלטפורמה) – אבל טקסט הטולטיפ בפועל **כן**
מתעדכן דינמית (`chrome.action.setTitle`) לפי השפה שנבחרה.

### הפעלה בלחיצה ימנית (context menu)
נוספו שתי אפשרויות בתפריט לחיצה ימנית:
- **"Capture Area"** – בכל מקום בעמוד, מתחיל בדיוק את אותו
  תהליך גרירת-בחירה כמו לחיצה על סמל התוסף.
- **"Extract Table from This Image"** – מופיע רק כשלוחצים ימנית *על תמונה*
  (`<img>`) – מדלג לגמרי על שלב הגרירה/הצילום ומריץ OCR ישירות על התמונה
  עצמה. זה נועד בדיוק למקרה שהתמונה כבר "מוכנה" (למשל דשבורד שהודבק כתמונה
  לתוך העמוד) – למה לגרור בחירה כשאפשר לחלץ ישירות?
  טכנית: התמונה נשלפת (`fetch`) מתוך **background.js**, לא מתוך ה-content
  script – כי לבקשות רשת מה-service worker יש הרשאת `host_permissions`
  שמדלגת על מגבלות CORS של העמוד המארח, בעוד בקשה זהה מתוך content script
  הייתה כפופה למדיניות ה-CORS של האתר המארח ועלולה להיכשל על תמונות
  ממקור חיצוני.

### מסך "ברוכים הבאים" בהתקנה ראשונה
נפתח אוטומטית (`chrome.tabs.create`) בפעם הראשונה שהתוסף מותקן
(`chrome.runtime.onInstalled`, `reason==="install"`), ומסביר בעברית/אנגלית
(עם החלפה חיה) את 4 הדרכים להתחיל צילום, ואת 4 השלבים עד הדבקה ב-Excel.

### קובץ התקנה (Install-Extractify.bat)
במקום להסביר למשתמש איך לעשות Load-unpacked ידנית, נוסף סקריפט `.bat`
(לא קובץ EXE קומפילי – נשאר קריא-לחלוטין כטקסט פשוט, בלי צורך בחתימה
דיגיטלית או "לעקוף" אזהרות אנטי-וירוס) שמבצע:
1. מעתיק את קבצי התוסף למיקום קבוע (`%LOCALAPPDATA%\Extractify`).
2. מאתר את כרום במחשב.
3. אם כרום סגור: מריץ אותו עם הדגל `--load-extension=<נתיב>` – דגל רשמי
   ומתועד של כרום שטוען תוסף לא-ארוז אוטומטית, בלי צורך לגעת ב-`chrome://
   extensions` בכלל. אם כרום פתוח, מבקש מהמשתמש לסגור ולהריץ שוב (לא סוגר
   את כרום בכוח – זו הייתה פעולה הרסנית פוטנציאלית שנמנעה ממנה בכוונה).

### עדכון בלי התקנה מחדש
כשמשתמשים ב-Load-unpacked (או בהתקנה האוטומטית), "עדכון" הוא פשוט: דורסים
את הקבצים הישנים בחדשים ולוחצים על סמל הרענון (↻) ליד התוסף ב-
`chrome://extensions`. `chrome.storage.sync` (ההגדרות) שורד את זה מעצמו.
נוסף מנגנון ש"תופס" את זה (`chrome.runtime.onInstalled`, `reason==="update"`)
ומציג באנר "Extractify was just updated to vX.X.X" בעמוד ההגדרות בפעם הבאה
שהוא נפתח – כדי שהמשתמש ידע שהעדכון אכן נקלט.
**זה נבחר במפורש על פני פרסום בחנות Chrome** (שהיה נותן עדכונים אוטומטיים
אמיתיים) כדי לשמור על הפצה פרטית לגמרי – ראו סעיף 5 למטה.

## גרסה 1.1.0 – מה השתנה מאז 1.0.0

**תיקון קריטי:** גרסה 1.0.0 כללה `"default_locale": "he"` ב-manifest.json
בלי תיקיית `_locales` מתאימה – זו הפרה של חוקי הוולידציה של כרום, וכרום
**מסרב לטעון תוסף כזה בכלל** (שגיאה בזמן "טעינת תוסף שלא נארז"). זה נמצא
ותוקן (הוסרה השורה, היא לא הייתה נחוצה מלכתחילה כי לא נעשה שימוש
ב-`__MSG_x__` placeholders בשום מקום) – ראו "בדיקות שבוצעו" למטה. **אם
התקנתם את גרסה 1.0.0 וזה לא עבד – זו הסיבה; בגרסה 1.1.0 זה תוקן.**

שאר השינויים הם שיפורי UI/UX ואמינות:
- **פס התקדמות אמיתי** בזמן הזיהוי (טעינת מנוע → טעינת נתוני שפה → זיהוי),
  במקום רק "מזהה טקסט…" סתום.
- **תמונה ממוזערת** של האזור שנבחר בפאנל, כדי לוודא בקלות שנבחר האזור הנכון.
- **תקציר תוצאה** ("זוהתה טבלה: 8 שורות · עד 3 עמודות") במקום רק "הצליח".
- **פאנל נגרר** (לחיצה והחזקה על הכותרת) כדי שלא יסתיר את התוכן שמתחתיו.
- **תווית מידות חיה** (רוחב × גובה בפיקסלים) בזמן גרירת הבחירה.
- **כפתור "אזור חדש"** בפאנל – צילום נוסף בלי לסגור ולפתוח מחדש.
- **מצב כהה (Dark mode)** לפאנל, לתמונה הממוזערת ולעמוד ההגדרות.
- **הודעות שגיאה ידידותיות** (למשל: "לא ניתן היה לטעון את קבצי מנוע ה-OCR"
  במקום הודעת שגיאת JavaScript גולמית).
- **בדיקת מנוע OCR** מובנית בעמוד ההגדרות – ראו "בדיקות שבוצעו".
- **חיזוק קוד**: לוגיקת ה-OCR ושחזור הטבלה אוחדה לקובץ משותף אחד
  (`lib/snaptable-ocr.js`) שגם content.js וגם עמוד ההגדרות משתמשים בו,
  כדי שלא יהיו שני מימושים שיכולים להתפצל אחד מהשני עם הזמן.

## הבעיה

נתונים רבים (במיוחד בדשבורדים פנימיים, מערכות ישנות, ותמונות/צילומי מסך
שמתקבלים במייל או ב-WhatsApp) נגישים רק כתמונה – אי אפשר לסמן ולהעתיק אותם
כטקסט, ובטח שלא כטבלה מסודרת לתוך Excel/Google Sheets. ההעתקה הידנית איטית
ומועדת לטעויות.

## הפתרון שנבחר

תוסף Chrome (Manifest V3) שמאפשר:
1. גרירת מלבן מעל כל אזור על המסך (כולל תוכן שהוא בעצמו תמונה/Canvas/iframe –
   כי החיתוך נעשה על צילום מסך של הפיקסלים, לא על ה-DOM).
2. הרצת OCR (זיהוי תווים) על האזור שנבחר.
3. שחזור מבנה טבלה (עמודות) מתוך מיקומי המילים בתמונה, והצגת התוצאה בפאנל
   לעריכה.
4. העתקה ללוח כטקסט מופרד בטאבים (TSV) – הפורמט ש-Excel ו-Google Sheets
   מפרשים אוטומטית כעמודות בהדבקה.

## החלטות מפתח (ומה שהיה כרוך בהן)

### 1. מנוע OCR: Tesseract.js – אופליין, מקומי בתוסף

**האפשרות שנבחרה** (לפי בקשת המשתמש): הרצת מנוע OCR מלא בתוך הדפדפן
([Tesseract.js](https://github.com/naptha/tesseract.js), מבוסס WebAssembly),
עם קבצי השפה (עברית + אנגלית) ארוזים בתוך התוסף.

**יתרונות:**
- לא דורש מפתח API, תשלום, או חיבור לאינטרנט בזמן ריצה.
- פרטיות מלאה – שום תמונה לא יוצאת מהמחשב של המשתמש.
- עובד גם ברשתות ארגוניות סגורות.

**החיסרון המרכזי:** התוסף "שוקל" יותר (כ-8MB, בעיקר קבצי מודל השפה וה-WASM),
ודיוק הזיהוי נמוך יותר בהשוואה ל-API ענן (Google Vision וכו') – בפרט על טקסט
מטושטש, זוויתי, או בפונטים חריגים. זו הייתה הפשרה מודעת מול האפשרות
השנייה (Cloud OCR API), שנדחתה כדי להימנע מתלות במפתח API ומעלות שוטפת.

**פרטי מימוש:**
- נעשה שימוש בגרסת המנוע `-lstm` בלבד (לא הגרסה שתומכת גם במנוע הישן
  "Legacy"), כי `oem=1` (LSTM_ONLY) הוא היחיד שבו נעשה שימוש – זה חוסך
  כ-9.5MB לעומת הכללת כל 4 הבילדים שהתיעוד הרשמי ממליץ עליהם.
- נבחרה הגרסה `best_int` (איכות גבוהה, קוונטיזציה ל-int) של קבצי השפה,
  לא `4.0.0` הרגילה (גדולה פי 3-4 בלי שיפור דיוק משמעותי לשימוש הזה) ולא
  `fast` (פחות מדויקת).

### 2. פורמט הדבקה: TSV (Tab-Separated) ולא HTML-טבלה

Excel ו-Google Sheets, כמו כל תוכנת גיליון, מפרשים טקסט עם תווי Tab בין
ערכים ו-Enter בין שורות כטבלה עם עמודות אמיתיות בהדבקה רגילה (Ctrl+V) –
בלי צורך בתוסף צד-שלישי בגיליון או בפורמט קובץ מיוחד. זו הדרך הפשוטה
והאמינה ביותר "להדביק כטבלה".

### 3. שחזור עמודות: היוריסטיקה מבוססת-מרווחים, לא זיהוי טבלה אמיתי

Tesseract לא "יודע" שיש טבלה בתמונה – הוא רק מחזיר מילים ומיקומן (bounding
box) בתמונה. התוסף:
1. ממיין את המילים בכל שורה לפי מיקום אופקי.
2. מחשב את המרווח בין כל שתי מילים סמוכות.
3. אם המרווח גדול (יחסית לגובה התו, לפי "רגישות" שניתן לכוון בהגדרות) –
   זה נחשב גבול עמודה (Tab). אחרת – זה רווח רגיל בתוך אותה תא.

**זו היוריסטיקה, לא קסם** – בטבלאות עם יישור עמודות לא אחיד, תאים ריקים,
או טקסט שמתפרס על כמה שורות בתוך תא אחד, התוצאה עלולה לדרוש תיקון ידני.
בדיוק בשביל זה הפאנל שמופיע לאחר הזיהוי הוא **ניתן לעריכה** לפני ההעתקה,
ולא מעתיק אוטומטית.

### 4. למה יש פאנל תצוגה מקדימה, ולא העתקה אוטומטית מיידית

שתי סיבות:
- **טכנית:** דפדפנים מרשים כתיבה ללוח (Clipboard) רק זמן קצר אחרי פעולת
  משתמש אמיתית (לחיצה/גרירה). זיהוי OCR לוקח שנייה-שתיים, ובמקרים רבים
  "החלון" הזה כבר נסגר עד שהתוצאה מוכנה – כתיבה אוטומטית ללוח הייתה נכשלת
  בשקט בהרבה מקרים. לחיצה על כפתור "העתק" בפאנל היא פעולת משתמש טרייה,
  ולכן אמינה.
- **מוצרית:** מאפשר לתקן שגיאות OCR (הן קורות, במיוחד בעברית) לפני
  ההדבקה בגיליון.

### 5. הפצה: קובץ ZIP להתקנה פרטית (Load unpacked), לא חנות Chrome

לפי בקשת המשתמש. משמעות: אין עדכונים אוטומטיים, וכל מחשב שרוצה להשתמש
בתוסף צריך לעבור את תהליך ההתקנה הידני (ראו README.md). אם בעתיד ירצו
להפיץ לכמה עובדים/מחשבים בקלות – פרסום כ"תוסף פרטי/לא רשום" בחנות
(Unlisted) הוא השדרוג הטבעי הבא (דורש חשבון מפתח גוגל, עמלה חד-פעמית
של כ-5$, וזמן בדיקה).

## ארכיטקטורה

```
סמל התוסף / קיצור מקלדת / לחיצה ימנית "Capture Area"
        │
        ▼
background.js (service worker)
  - מזריק overlay.css + tesseract.min.js + extractify-i18n.js + snaptable-ocr.js
    + content.js לטאב הפעיל
  - שולח הודעת "SNAPTABLE_START"
        │
        ▼                                    לחיצה ימנית "Extract Table
content.js (רץ בתוך העמוד)                    from This Image" (על <img>)
  - מציג שכבת בחירת אזור (drag-to-select)              │
  - במעבר עכבר: מבקש מ-background צילום מסך            ▼
    (chrome.tabs.captureVisibleTab)              background.js: fetch() את
  - חותך את התמונה לאזור שנבחר (Canvas)           ה-src של התמונה, ממיר ל-
        │                                        data URL, מזריק את אותם
        ▼                                        קבצים + שולח הודעת
  מריץ Tesseract.js (worker.min.js +             "SNAPTABLE_EXTRACT_IMAGE"
  tesseract-core-*.wasm.js + נתוני שפה,                  │
  כולם מקומיים בתוסף – שום קריאת רשת החוצה)             │
        │                                                │
        ▼                                                ▼
  משחזר מבנה טבלה מתוך מיקומי המילים          content.js מריץ ישירות OCR
        │                                      על התמונה (בלי גרירה/צילום)
        ▼                                                │
  מציג פאנל לעריכה + כפתורי העתקה  ◄──────────────────────┘
  (טקסט הפאנל כולו דרך extractify-i18n.js, כיוון RTL/LTR נקבע דינמית)
```

כל הריצה (זיהוי אזור, חיתוך, OCR) קורית בתוך ה-content script של הדף –
ה-worker של Tesseract.js נטען מכתובת `chrome-extension://…` ולכן פועל
תחת מדיניות האבטחה (CSP) של התוסף עצמו, לא של האתר המארח – כך שהתוסף
עובד גם באתרים עם מדיניות CSP מחמירה.

## מותג ולוגו

שם: **Extractify** ("SnapTable OCR" → "Tablify" ב-1.2.0 → "Extractify"
ב-1.3.0 – ראו למעלה). לוגו:
מסגרת "עדשת צילום אזור" (ויזואל מוכר מכלי Screenshot) עם רשת טבלה קטנה
במרכז, בגרדיאנט סגול-כחול – נשמר כפי שהוא כי הוא מושגי/מופשט ומתאים לכל
שם. נוצר פרוגרמטית (`make-icons.ps1`, System.Drawing) בגדלים
16/32/48/128 פיקסלים.

## מגבלות ומה שכדאי לדעת

- **דיוק OCR**: תלוי איכות/רזולוציית הצילום. טקסט קטן מאוד, מטושטש, או עם
  ניגודיות נמוכה (למשל טקסט אפור בהיר על רקע לבן) ייתן תוצאות פחות טובות –
  מומלץ להגדיל את חלון הדפדפן/הזום לפני הבחירה אם אפשר.
- **שחזור עמודות הוא היוריסטי**, לא מבוסס זיהוי קווי טבלה אמיתיים – ייתכנו
  פיצולים/מיזוגים שגויים בטבלאות לא סטנדרטיות. ניתן לכוון "רגישות" בהגדרות,
  ותמיד אפשר לתקן ידנית בפאנל לפני ההעתקה.
- **דפים עם `<video>`/WebGL מוגן תוכן (DRM)**: `chrome.tabs.captureVisibleTab`
  לא תמיד תופס תוכן כזה (מגבלת דפדפן, לא של התוסף).
  לרוב לא רלוונטי לדשבורדים/טבלאות.
  - **PDF שנפתח בתצוגת ה-PDF המובנית של כרום**: לפעמים content scripts לא
  פועלים שם כברירת מחדל – ייתכן שיהיה צורך לאפשר גישה ל"קובצי PDF" בהגדרות
  התוסף (`chrome://extensions` → פרטי → "Allow access to file URLs" אם
  מדובר בקובץ מקומי).
- **טעינה ראשונה איטית יותר**: בפעם הראשונה שמריצים OCR בכל טעינת עמוד,
  המנוע (worker + WASM + נתוני שפה) נטען לזיכרון – לוקח כשנייה-שתיים נוספות.
  צילומים הבאים באותו טאב מהירים יותר (ה-worker נשמר בזיכרון).
- **אין תמיכה בשפות נוספות** מעבר לעברית/אנגלית כרגע (אפשר להוסיף – ראו
  "המשך אפשרי" למטה). זה נכון גם לשפות OCR וגם לשפת הממשק.
- **מזהה התוסף (Extension ID) אינו קבוע**: תוסף לא-ארוז (unpacked) בלי
  `"key"` חתום ב-manifest מקבל ID חדש בכל פעם שמתקינים אותו מחדש מאפס
  (הסרה + הוספה מחדש). לחיצה על **"Reload"** (↻) לאחר דריסת קבצים **כן**
  שומרת על אותו ID ועל ה-`chrome.storage.sync` הקיים – זו הדרך המומלצת
  לעדכן, לא הסרה+התקנה מחדש.
- **שם התוסף ברשימת `chrome://extensions` קבוע לאנגלית** (מוגדר ב-manifest
  ב"זמן בנייה") – מתג השפה בעמוד ההגדרות משפיע על הטולטיפ (מתעדכן דינמית),
  על עמוד ההגדרות, ועל הפאנל, אבל לא על השם הרשום הזה – מגבלת פלטפורמה
  של כרום, לא של התוסף.

## בדיקות שבוצעו

חשוב להבדיל בין שני סוגי בדיקות שבוצעו על התוסף:

### 1. בדיקות סטטיות (על כל הקבצים)
- **תקינות JSON**: `manifest.json` נבדק עם JSON.parse בפועל (לא רק "נראה
  תקין") – ותוך כדי כך **התגלה ותוקן** הבאג הקריטי של `default_locale`
  שתואר למעלה, שהיה גורם לכרום לסרב לטעון את התוסף.
- **תקינות JavaScript**: כל קובצי ה-JS (`background.js`, `content.js`,
  `snaptable-ocr.js`, `options.js`) נבדקו עם `node --check` (בדיקת תחביר)
  אחרי כל שינוי.
- **קריאת קוד המקור בפועל** של Tesseract.js (לא רק התיעוד): וידאתי מול
  הקוד הממוזער בפועל (`worker.min.js`) איך הוא בונה את הנתיב לקובץ ה-WASM
  (`corePath` + `/tesseract-core-simd-lstm.wasm.js` או הגרסה הלא-SIMD, לפי
  `oem`), ואיך הוא בונה את נתיב קובצי השפה (`langPath` + `<lang>.traineddata.gz`,
  כולל טיפול מפורש בנתיבי `chrome-extension://`). זה מה שנתן ביטחון
  שהאינטגרציה תעבוד *לפני* שהורצה בפועל.

### 2. בדיקה פונקציונלית אמיתית (בדפדפן, לא רק סטטית)

הורצה בדיקה אמיתית של שרשרת ה-OCR המלאה, מחוץ להקשר של תוסף כרום (כדי
שאפשר יהיה להריץ אותה כאן ולראות תוצאה), אך תוך שימוש **בקבצים המדויקים
שנארזים בתוסף** (`tesseract.min.js`, `snaptable-ocr.js`, הליבה של
Tesseract, נתוני השפה) ללא שום שינוי בהם – רק "לחשן" קטן שמדמה את
`chrome.runtime.getURL` כך שהקבצים ייטענו משרת מקומי במקום מתוך התוסף.
ההרצה עצמה (כלי `test/harness.html`, ראו שם) הוכיחה:

| בדיקה | תוצאה |
|---|---|
| טעינת מנוע ה-OCR (WASM) + נתוני שפה עברית+אנגלית | ✅ הצליחה, ~290ms |
| זיהוי "4217" ו-"9080" בתמונת בדיקה עם רווח גדול ביניהם | ✅ זוהו במדויק |
| שחזור טבלה: זיהוי 2 עמודות ו-Tab במקום הנכון | ✅ `"4217\t9080"` |
| זיהוי טקסט עברי אמיתי ("שלום עולם") | ✅ זוהה **במדויק**, ביטחון 92% |

זו הוכחה קונקרטית (לא רק היסק תיאורטי) שהחלק הכי מורכב ובעל הכי הרבה
פוטנציאל לתקלות בתוסף – חיווט מנוע ה-Tesseract.js מול הקבצים המקומיים –
עובד בפועל בדפדפן אמיתי.

**מה עדיין לא נבדק באופן אינטראקטיבי (ולמה):** ה-API-ים הספציפיים
לתוסף-כרום עצמו (`chrome.tabs.captureVisibleTab`, `chrome.scripting`,
`chrome.commands`, טעינת "Load unpacked" בפועל) לא הורצו אינטראקטיבית,
כי זה דורש התקנה בתוך פרופיל כרום אמיתי (עם דיאלוג בחירת תיקייה ברמת
מערכת ההפעלה) – פעולה שלא בוצעה בלי תיאום מראש איתך. אלה API-ים סטנדרטיים
ומתועדים היטב שנבדקו מול התיעוד הרשמי, אך מומלץ שתעברו על "בדיקת קבלה
ידנית" קצרה בעצמכם אחרי ההתקנה (ראו למטה) – ובנוסף, לחצו על "הרצת בדיקה"
בעמוד ההגדרות: זו בדיוק אותה בדיקת OCR, אבל רצה *בתוך* התוסף המותקן שלכם,
כך שהיא גם בדיקת קבלה לתוסף המותקן בפועל אצלכם, לא רק אצלי.

### 3. בדיקות נוספות שבוצעו עבור גרסה 1.2.0

- **שלמות מילון התרגום**: נבדק פרוגרמטית (Node) ש-105 המפתחות בעברית
  ו-105 המפתחות באנגלית תואמים אחד לשני בדיוק (אין מפתח קיים בשפה אחת
  וחסר בשנייה) – מונע מצב של טקסט חסר/נופל-חזרה לשפה הלא-נכונה.
- **בדיקה חזותית אמיתית בדפדפן** (לא רק קריאת קוד) של עמוד ההגדרות ומסך
  "ברוכים הבאים": נטענו בפועל בדפדפן (עם "לחשן" קטן ל-API-ים של כרום,
  באותה שיטה כמו בדיקת ה-OCR), כולל **מעבר בין אנגלית לעברית תוך כדי
  תצפית** – אומת שהפריסה מתהפכת נכון (RTL מלא: כיווניות, יישור, מיקום
  התפריטים), שהטקסט המתורגם נכון בשתי השפות, ושבאנר "עודכן לגרסה" ולחצן
  "הרצת בדיקה" ממשיכים לעבוד אחרי החלפת שפה. **תוך כדי הבדיקה הזו נמצאו
  ותוקנו 2 טעויות הקלדה בעברית** (מילה כפולה, ותו שגוי) שלא היו מתגלות
  בקריאת קוד בלבד.
- **בדיקת המנוע עצמה הורצה מחדש** דרך עמוד ההגדרות (לא רק דרך ה-harness
  העצמאי) – גם באנגלית וגם בעברית – ועברה ✅ בשתי השפות (195ms, זיהוי מדויק
  של "4217 9080", 2 עמודות).
- **מה עדיין לא נבדק אינטראקטיבית**: תפריט הלחיצה הימנית (`chrome.contextMenus`)
  ומסך "ברוכים הבאים" בהקשר install אמיתי, ותסריט ה-`Install-Extractify.bat`
  עצמו על מכונה אמיתית – שוב, כי זה דורש התקנה בפרופיל כרום אמיתי. הקוד
  נבדק תחבירית ונקרא נגד תיעוד ה-API הרשמי של `chrome.contextMenus` /
  `chrome.tabs.create` / דגל השורת-פקודה `--load-extension`.

### בדיקת קבלה ידנית מומלצת (כמה דקות)
1. התקינו לפי README.md (מומלץ לנסות את `Install-Extractify.bat`).
2. ודאו שמסך "ברוכים הבאים" נפתח אוטומטית; לחצו על "עב" ובדקו שהכול
   מתהפך ל-RTL כמו שצריך, ואז "Open Settings".
3. בעמוד ההגדרות, לחצו "Run Test" (או "הרצת בדיקה") – אמור להופיע ✅ תוך
   כמה שניות.
4. נסו לצלם טבלה אמיתית קטנה (למשל מתוך Excel פתוח בדפדפן, או כל דשבורד)
   בכל אחת מ-4 הדרכים (סמל, קיצור, לחיצה ימנית על העמוד, לחיצה ימנית על
   תמונה) ולוודא שהעתקה-הדבקה ל-Excel/Sheets נכנסת לעמודות נכונות.
5. נסו `Esc` באמצע בחירה, ולחצו על סמל התוסף פעמיים ברצף – ולוודא שלא
   נשארים כמה פאנלים/שכבות בחירה על המסך בו-זמנית.
6. חלצו קובץ ZIP "מעודכן" (אותו קובץ ממש, גם אם הגרסה זהה) מעל התיקייה
   הקיימת, לחצו ↻ (Reload) ב-`chrome://extensions`, וודאו שההגדרות שלכם
   (שפה, שפות OCR) לא התאפסו.

## המשך אפשרי (לא ממומש כרגע)

- הוספת שפות OCR נוספות (ערבית, רוסית וכו') – דורש הורדת קבצי
  `.traineddata` נוספים והוספת אפשרות בחירה בהגדרות. **שים לב**: זה שונה
  משפת הממשק (שכבר תומכת באנגלית/עברית) – זו תמיכה בזיהוי טקסט בשפות
  נוספות בתוך התמונה עצמה.
- הוספת שפות ממשק נוספות (מעבר לאנגלית/עברית) – התשתית (`extractify-i18n.js`)
  כבר בנויה כך שקל להוסיף מילון שפה שלישי.
- שיפור זיהוי הטבלה ע"י שילוב זיהוי קווי גריד בתמונה (edge detection) לפני
  ה-OCR, לא רק היוריסטיקת מרווחים.
- מצב "זיהוי טבלה מרובת עמודות עם תאים ריקים" (כרגע תא ריק לא תמיד מזוהה
  נכון כי אין "עוגן" ויזואלי לזהות שהיה שם תא).
- הוספת `"key"` חתום קבוע ל-manifest.json כדי ש-Extension ID יישאר יציב
  גם בהסרה+התקנה מחדש (לא רק ב-Reload) – לא נחוץ כרגע כי Reload מספיק
  לתרחיש השימוש הנוכחי.
- פרסום כ"תוסף לא רשום" (Unlisted) בחנות Chrome להפצה קלה בין כמה מחשבים,
  כולל עדכונים אוטומטיים אמיתיים – **נשקל במפורש ונדחה לטובת הפצה פרטית**
  (ראו סעיף 5 בהחלטות המפתח וגרסה 1.2.0 למעלה); עדיין אופציה זמינה בעתיד
  אם יתעורר צורך בהפצה לכמה משתמשים בקלות.
- **"ניקוי" של הטקסט המזוהה ע"י AI בענן** (LLM כמו Claude/ChatGPT שהיה
  יכול לתקן שגיאות OCR, לנחש עמודות חסרות, לתרגם וכו') – **נשקל במפורש
  ונדחה כרגע**: זה ידרוש חיבור אינטרנט, מפתח API, ותשלום שוטף — בסתירה
  ישירה לעמדת הפרטיות/אופליין המרכזית של המוצר (ראו גרסה 1.4.0 למעלה).
  אם ירצו בעתיד, הדרך הנכונה תהיה כאפשרות **אופציונלית ומכובה כברירת
  מחדל** בהגדרות (opt-in מפורש), לא התנהגות אוטומטית.
